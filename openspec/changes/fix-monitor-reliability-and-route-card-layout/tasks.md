## 1. 結果卡片右側候車資訊塊

- [x] 1.1 將 `item_bus_route.xml` 的卡片主內容改為 `ConstraintLayout` 或等效約束結構，保留卡片外觀、圓角、描邊、padding 和底部分隔線樣式。
- [x] 1.2 將路線號約束到左側第一行，將站點預覽約束到左側第二行，站點預覽使用剩餘寬度並保持單行省略。
- [x] 1.3 新增或重排右側候車資訊塊，使其跨越路線號與站點預覽兩行高度，包含主候車狀態和可用時的下一班摘要。
- [x] 1.4 將小鈴鐺入口移入右側候車資訊塊或其最右側，只調整位置，不修改圖示、顏色、透明背景、padding 和 48dp 觸控區語義。
- [x] 1.5 保持候車區點擊打開 ETA 班次面板、小鈴鐺點擊啟動監控、卡片其他區域點擊路線詳情三者互不干擾。
- [x] 1.6 檢查窄屏、長站點名稱、多段路線名和大字體下，路線號、站點預覽、候車資訊、鈴鐺和底部信息不重疊。

## 2. 鎖屏可見通知渠道

- [x] 2.1 新增符合鎖屏可見要求的監控通知 channel id，避免依賴已建立的舊低重要性 channel 原地升級。
- [x] 2.2 為監控通知設置 public lockscreen visibility，並在 channel 上設置對應鎖屏可見性。
- [x] 2.3 保持監控通知為 ongoing notification，展示路線號、監控狀態、剩餘候車分鐘、下一班、步行到站和最後更新時間。
- [x] 2.4 確認通知 channel 不使用不必要的震動、聲音或全屏 intent；狀態提醒 channel 只在 `立即出門` 或 `快遲到了` 等必要狀態提高提醒權重。
- [x] 2.5 處理通知權限缺失或被拒絕時的提示與降級，確保不啟動無可見通知的監控服務。

## 3. 監控 Session 持久化

- [x] 3.1 設計並新增監控 session 持久化模型，保存 route name、首程 ETA query、步行到站分鐘、語音開關、session 起始時間和最大有效時長。
- [x] 3.2 在監控開始時寫入 session，在用戶手動停止、自動停止或連續失敗保護觸發時清理 session。
- [x] 3.3 每次 ETA 刷新成功後保存最後成功通知文案、最後更新時間、當前狀態、最後已播報狀態、第 1 班和第 2 班 ETA 時間。
- [x] 3.4 服務重建或調度喚醒時從持久化 session 恢復監控資料；若 session 已過停止條件或最大時長，清理並停止通知。
- [x] 3.5 避免將完整 `BusRouteOption` 或 UI 派生資料持久化，只保存恢復監控所需的穩定字段。

## 4. 退前台與鎖屏刷新調度

- [x] 4.1 抽取監控刷新調度器，統一管理下一次刷新安排、取消、防重入和停止時清理。
- [x] 4.2 保留服務活躍時的 60 秒 `Handler` tick 作為前台或進程活躍時的快速路徑。
- [x] 4.3 增加 `AlarmManager` 或等效系統調度，在 App 退前台、鎖屏或服務被重建時仍可喚醒並嘗試刷新 ETA。
- [x] 4.4 優先使用不需要特殊權限的 idle-aware/inexact 調度；只有在實測不可接受時才評估 exact alarm 與額外權限。
- [x] 4.5 調度觸發後只刷新首程 ETA，不重跑完整點到點路線搜尋，並設置請求超時、失敗降級和下一次調度。
- [x] 4.6 在 Doze、省電或網絡限制導致延遲時，保留最後成功通知文案並展示資料延遲或最後更新時間。
- [x] 4.7 確認用戶透過系統強制停止 App 或前台服務時，不嘗試繞過系統限制自動復活；下次 App 啟動時清理或標記 session 中斷。

## 5. 語音播報可靠性

- [x] 5.1 抽取 TTS 管理器或 helper，封裝初始化、語言設定、fallback、試聽、播報和釋放。
- [x] 5.2 檢查 TextToSpeech 初始化結果和 `setLanguage` 返回值，按繁中、香港中文、台灣中文、通用中文或實作定義順序 fallback。
- [x] 5.3 在監控設定底部面板新增 `試聽語音` 入口，使用當前語音 helper 播放一段示例提醒。
- [x] 5.4 當 TTS 引擎或語言不可用時，在面板或狀態中提示語音不可用，但允許用戶繼續只使用通知監控。
- [x] 5.5 將播報觸發綁定到監控狀態機變更，使用持久化的最後已播報狀態避免同一狀態重複播報。
- [x] 5.6 在 App 退前台或鎖屏後狀態切換時仍嘗試播報；播放被系統音頻策略拒絕時不阻塞通知更新。
- [x] 5.7 確保停止監控、服務銷毀和 App force-stop 後釋放 TTS 資源。

## 6. 集成與行為邊界

- [x] 6.1 更新通知文案，清楚展示 `準備出門`、`立即出門`、`快遲到了`、資料延遲和更新失敗狀態。
- [x] 6.2 保持第 2 班 ETA 過站後自動停止；缺少第 2 班 ETA 時維持第 1 班過站後 `+2 分鐘` fallback。
- [x] 6.3 保持通知 `刷新`、`停止`、`打開 App` action 可用，並與持久化 session 和調度器狀態一致。
- [x] 6.4 更新 App 內提示文案，將系統限制表達為「每分鐘嘗試更新」，避免承諾所有省電狀態下精確 60 秒刷新。
- [x] 6.5 檢查 manifest 權限與 foreground service type，避免新增不必要的高風險權限；若必須新增權限，需在設計和 UI 提示中說明。

## 7. 測試與驗證

- [x] 7.1 補充卡片 layout/helper 測試，覆蓋右側跨兩行候車資訊塊、站點預覽省略、鈴鐺樣式不變和點擊事件互不干擾。
- [x] 7.2 補充通知 channel 和 notification builder 測試或可驗證 helper 測試，覆蓋鎖屏 visibility、channel id 遷移和完整通知文案。
- [x] 7.3 補充監控 session 持久化測試，覆蓋開始、刷新成功、服務重建恢復、停止條件清理和中斷標記。
- [x] 7.4 補充刷新調度器測試，覆蓋 Handler tick、AlarmManager 調度、防重入、取消、連續失敗和資料延遲。
- [x] 7.5 補充 TTS helper 測試，覆蓋語言 fallback、不可用降級、試聽入口和同一狀態不重複播報。
- [x] 7.6 執行 `openspec validate fix-monitor-reliability-and-route-card-layout --strict`。
- [x] 7.7 執行 `./gradlew testDebugUnitTest`。
- [x] 7.8 執行 `./gradlew build`。
- [ ] 7.9 使用模擬器或真機驗證主頁卡片 UI：正常字體、大字體、長站名、多段路線、候車區點擊、鈴鐺點擊和卡片詳情入口。
- [ ] 7.10 使用模擬器或真機驗證通知監控：通知權限、鎖屏可見、退前台後更新、鎖屏後更新、通知 action、資料延遲狀態和停止清理。
- [ ] 7.11 使用模擬器或真機驗證語音：試聽、狀態切換播報、語音關閉、TTS 不可用降級，以及鎖屏或退前台後播報行為。
- [ ] 7.12 使用 `adb shell dumpsys deviceidle force-idle` 或等效方式驗證 Doze 限制下的降級行為，記錄實際刷新延遲並確認通知文案不誤導。
