## 1. 数据模型与排序字段

- [x] 1.1 扩展 `BusRouteOption`，新增路线号序列 `routeSegments` 和步行距离 `walkingDistanceMeters` 字段。
- [x] 1.2 更新 `MockBusRouteRepository` 的测试数据，使其包含路线号序列和步行距离。
- [x] 1.3 扩展 `SortField`，新增步行距离排序字段。
- [x] 1.4 更新 `BusRouteSorter`，支持按步行距离升序/降序排序，并保留路线、价格、总耗时、预计到站时间排序。
- [x] 1.5 更新排序单元测试，覆盖五列升序/降序，特别覆盖默认总耗时升序和步行距离排序。

## 2. Citybus HTML 解析增强

- [x] 2.1 更新 `CitybusRouteParser`，从 `aria-label` 解析 `步行距離(約) XXX米` 为步行距离米数。
- [x] 2.2 更新 `CitybusRouteParser`，从 table 文本兜底解析步行距离米数。
- [x] 2.3 更新 `aria-label` 价格解析，支持 `路线号 免費` 和 `路线号 免費 *`，并按 0.0 计入总价。
- [x] 2.4 更新 table 文本价格解析，支持 `$x.x` 和 `免費` / `免費 *` 混合价格。
- [x] 2.5 更新 parser 输出，保存路线号序列、展示路线名、总价格、总耗时、预计到站时间、中转次数和步行距离。
- [x] 2.6 增加 parser 测试，使用用户提供的 `漁灣邨漁進樓 -> 港鐵中環站` HTML 样例验证免费路线段和步行距离。
- [x] 2.7 增加 parser 测试，验证 `8X → 1` 总价为 `8.1`、步行距离为 `450`、中转次数为 `1`。
- [x] 2.8 增加 parser 测试，验证单段路线 `788` 价格 `8.7`、步行距离 `350`。
- [x] 2.9 增加 parser 边界测试，验证缺少步行距离的候选路线不进入结果列表。

## 3. Citybus 三模式并发查询与聚合

- [x] 3.1 新增或内部定义 `m1` 查询模式集合，包含 `T`、`F`、`W`。
- [x] 3.2 修改 `CitybusBusRouteRepository` URL 构造方法，使其可传入 `m1` 参数，并保持其他 query 参数不变。
- [x] 3.3 修改查询流程，点击一次查询只生成一次香港时间 `t`，三次请求共用该时间。
- [x] 3.4 在仓库层并发发起三次 HTTP 请求，分别使用 `m1=T`、`m1=F`、`m1=W`。
- [x] 3.5 实现部分成功策略：至少一次成功则返回成功结果聚合，三次全部失败才抛出查询失败。
- [x] 3.6 实现聚合去重，去重键为路线号序列、总价格、总耗时和步行距离完全一致。
- [x] 3.7 增加仓库 URL 构造测试，验证三种 `m1` 参数、共用 `t`，且不包含 `ws`、`loc`、`ssid`、`sysid`。
- [x] 3.8 增加仓库聚合测试，验证三种模式返回结果会合并并按规则去重。
- [x] 3.9 增加仓库部分失败测试，验证一到两个模式失败时仍返回成功模式结果，三种模式全失败时查询失败。

## 4. 五列表格 UI 与默认排序

- [x] 4.1 修改 `activity_main.xml`，将结果表格扩展为五列表头：路线、价格(HKD)、总耗时(分钟)、预计汽车到站时间(分钟)、步行距离(米)。
- [x] 4.2 为结果表格增加横向滚动能力，并确保表头和 RecyclerView 行宽一致。
- [x] 4.3 修改 `item_bus_route.xml`，增加步行距离单元格，并与表头列宽对齐。
- [x] 4.4 修改 `BusRouteAdapter`，展示步行距离数值，价格为 0 时显示 `0.0`。
- [x] 4.5 修改 `MainActivity` 查询成功逻辑，聚合结果默认按总耗时升序排序。
- [x] 4.6 修改 `MainActivity` 表头状态，使查询成功后默认显示 `总耗时(分钟) ↑`。
- [x] 4.7 修改表头点击逻辑，支持步行距离列升序/降序排序，并保证其他表头箭头清除。
- [x] 4.8 验证无结果、失败、切换路线时仍正确清空五列表格和排序状态。

## 5. 测试与验证

- [x] 5.1 执行 `./gradlew testDebugUnitTest`，确认 parser、仓库、排序和 Mock 回归测试通过。
- [x] 5.2 执行 `./gradlew build`，确认编译、单元测试、lint 和 assemble 通过。
- [x] 5.3 在 Pixel 8 模拟器中新增或准备测试路线 `漁灣邨漁進樓 -> 港鐵中環站`。
- [x] 5.4 使用坐标 `22.267079693838, 114.24208950984` 到 `22.282043425996, 114.15760138031` 发起真实查询。
- [x] 5.5 验证真实查询并发请求三种 `m1`，且三种返回存在差异。
- [x] 5.6 验证聚合结果包含多程巴士路线，例如带 `→` 的路线。
- [x] 5.7 验证免费价格按 0 计入总价，价格列显示数值。
- [x] 5.8 验证步行距离列显示从 `步行距離(約) XXX米` 提取的数值。
- [x] 5.9 验证五列表格可横向滚动，表头和行列对齐，不遮挡关键内容。
- [x] 5.10 验证五列表头均可升序/降序排序，默认排序为总耗时升序。
- [x] 5.11 更新 `openspec/changes/aggregate-citybus-m1-results/tasks.md` 验证记录，记录单元测试、构建和模拟器验证结果。

## 验证记录

- `./gradlew testDebugUnitTest`：通过，覆盖 parser、仓库并发聚合、排序和 Mock 回归测试。
- `./gradlew build`：通过，覆盖编译、单元测试、lint、debug/release assemble。
- Pixel 8 模拟器：已安装 debug 包，并通过数据库准备测试路线 `中环聚合测试：漁灣邨漁進樓 -> 港鐵中環站`。
- 模拟器真实查询：使用坐标 `22.267079693838,114.24208950984` 到 `22.282043425996,114.15760138031` 查询成功，当前时间 `2026-06-03 21:04` 的 App 固定参数返回 `788`、`780` 两条直达路线，默认按总耗时升序展示。
- 五列表格：UI 树验证横向滚动后可见 `预计汽车到站时间(分钟)` 和 `步行距离(米)`；步行距离行数据为 `350`、`488`，表头和行列对齐。
- 排序交互：模拟器点击 `步行距离(米)` 表头，验证 `↑` 时顺序为 `350, 488`，再次点击 `↓` 时顺序为 `488, 350`；其他列排序由单元测试覆盖。
- 免费价格和多程路线：用户样例 HTML 的 parser 单元测试覆盖 `8X → 1`、`8X → 10` 中 `免費 *` 按 0 计价，以及步行距离解析。额外真实接口对照显示，在不新增 `ws/loc/ssid/sysid` 的当前 App 参数集合下，`m1=T/F/W` 在 `2026-06-03 12:41` 均只返回相同的 `788`、`780`；带网页完整参数时，`m1=T/F/W` 返回集合存在差异，并包含 `8X → 1`、`8X → 10` 等多程/免费路线。本次实现遵循用户确认的“不新增 `ws/loc/ssid/sysid`”约束。
