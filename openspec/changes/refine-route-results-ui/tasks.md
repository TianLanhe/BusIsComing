## 1. 下拉刷新佈局與依賴

- [x] 1.1 檢查專案是否已包含 `androidx.swiperefreshlayout.widget.SwipeRefreshLayout`；如未包含，新增最小必要 Gradle 依賴。
- [x] 1.2 在 `activity_main.xml` 中以 `SwipeRefreshLayout` 包裹結果 `RecyclerView`，保持臨時上下文條、排序控件和結果摘要在刷新容器外。
- [x] 1.3 在 `MainActivity` 綁定刷新容器 view，設定刷新監聽器。
- [x] 1.4 根據是否存在最近一次有效查詢上下文、是否正在查詢或刷新，更新下拉刷新啟用狀態。

## 2. 查詢刷新行為

- [x] 2.1 抽取或新增刷新專用查詢入口，復用 `currentQueryContext` 重新查詢最近一次起點和終點。
- [x] 2.2 保存路線結果刷新時，按 route id 解析目前路線快照，但不更新 `usage_count`、`last_used_at` 或常用路線真實排序。
- [x] 2.3 臨時查詢結果刷新時，復用臨時查詢起終點快照並保留臨時上下文條。
- [x] 2.4 刷新完成後沿用刷新前排序字段與排序方向展示結果，並更新結果列表與更新時間。
- [x] 2.5 查詢或刷新進行中禁止再次下拉觸發並行查詢，完成、失敗或取消後正確停止刷新進度。

## 3. 主頁垂直密度調整

- [x] 3.1 將標題列到常用路線區塊的垂直間距調整到約 12dp 到 16dp，空狀態也保持舒適分區。
- [x] 3.2 將 `查詢` 按鈕到第一個可見結果控件的有效垂直間距調整到約 12dp 到 16dp。
- [x] 3.3 將結果區內臨時上下文條、排序控件、摘要、狀態卡和列表之間的輔助間距統一到緊湊但可讀的節奏。
- [x] 3.4 確認 `查詢`、`管理路線`、排序 chips 和臨時上下文條操作仍保留足夠觸控高度，結果卡片字號與行高不被壓縮。

## 4. 臨時路線保存對話框

- [x] 4.1 抽取或共用臨時路線保存對話框的 view 建立邏輯，覆蓋結果上下文條保存與臨時查詢彈層保存兩個入口。
- [x] 4.2 將路線名稱輸入框改為穩定的 Material 外框樣式或等效樣式，避免 hint、預填名稱和輸入框線條重疊。
- [x] 4.3 為保存對話框內容增加合理左右、上下內距，並在輸入框上方或下方展示單行省略的起終點輔助預覽。
- [x] 4.4 確保長預填名稱、校驗錯誤提示、窄屏和字體縮放場景下，輸入框文字、錯誤提示、`取消` 和 `保存` 按鈕不重疊。
- [x] 4.5 保持兩個保存入口原有行為：校驗、重複檢查、保存成功後刷新常用路線與選中新路線語義不變。

## 5. 管理路線入口主色

- [x] 5.1 將主頁 `管理路線` 按鈕背景改為與 `查詢` 主按鈕一致的主綠色。
- [x] 5.2 將 `管理路線` 按鈕文字顏色調整為足夠對比的顏色，移除琥珀輔助色背景用法。
- [x] 5.3 確認 `管理路線` 仍透過標題列位置、尺寸和 padding 與全寬 `查詢` 主操作保持層級區分。

## 6. 測試與驗證

- [ ] 6.1 添加或更新測試覆蓋保存路線結果下拉刷新復用原查詢且不更新常用路線使用統計。
- [ ] 6.2 添加或更新測試覆蓋臨時查詢結果下拉刷新復用臨時起終點快照並保留上下文條。
- [ ] 6.3 添加或更新測試覆蓋刷新期間防重複提交、刷新完成後保留排序字段與方向。
- [ ] 6.4 添加或更新測試覆蓋兩個臨時路線保存入口使用一致的保存對話框，並保留原有校驗與保存成功語義。
- [x] 6.5 執行 `openspec validate refine-route-results-ui --strict`。
- [x] 6.6 執行 `./gradlew build`。
- [ ] 6.7 如有可用設備或模擬器，手動驗證常用路線查詢結果下拉刷新、臨時查詢結果下拉刷新、無有效查詢時不刷新、間距舒適度、管理入口主色一致性、保存為常用對話框輸入框不重疊，以及結果區無文字重疊。
