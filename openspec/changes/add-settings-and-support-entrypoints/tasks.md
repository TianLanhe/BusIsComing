## 1. 首頁入口調整

- [x] 1.1 更新 `activity_main.xml` 的普通主頁頂部操作區：保留 `乘車碼`，移除頂部 `管理路線` 文字按鈕，新增右上角白底描邊圓形 `設定` 圖示入口。
- [x] 1.2 更新首次引導頁頂部操作區：保留 `乘車碼`，新增同款 `設定` 圖示入口，確認沒有保存路線時仍可進入設定、關於與隱私政策。
- [x] 1.3 在 `常用路線` 標題行保留 `全部` 文字入口，並新增獨立 `管理路線` 圖示入口；兩者保持獨立觸控區、清楚間距與無障礙描述 `管理路線`。
- [x] 1.4 調整 `MainActivity` 點擊邏輯：設定圖示打開設定頁，管理圖示打開既有路線管理頁，`全部` 仍打開完整常用路線列表，互不觸發其他流程。
- [x] 1.5 檢查首頁普通狀態、首次引導狀態、無保存路線但有臨時查詢上下文狀態下的入口顯示規則，確保不重疊、不新增額外一整行操作列。

## 2. 設定與關於頁 UI

- [x] 2.1 新增設定頁 Activity 或等效頁面，註冊到 `AndroidManifest.xml`，使用與現有頁面一致的淡綠根背景、白色實體列表面和左上返回入口。
- [x] 2.2 實作設定頁 App 資訊區，展示 `BusIsComing` 與目前 App 版本。
- [x] 2.3 實作設定頁 `偏好`、`支援`、`關於` 三組入口，順序為 `語言`、`分享應用`、`問題反饋`、`應用評分`、`檢查更新`、`關於我們`、`隱私政策`。
- [x] 2.4 新增關於我們二級頁或等效頁面，展示 `BusIsComing`、目前 App 版本、簡介 `保存常用路線，出門前快速比較巴士方案。` 與官網入口 `https://www.busiscoming.com`。
- [x] 2.5 補充必要字串、drawable/vector icon、contentDescription 與尺寸資源，確保設定圖示、管理圖示和列表入口在窄屏及字體放大時保持可讀可點。

## 3. 設定入口行為

- [x] 3.1 實作 `語言`、`應用評分`、`檢查更新` 點擊後的暫不支援 Toast：`暫不支援語言切換`、`暫不支援應用評分`、`暫不支援檢查更新`，且不觸發真實 locale、商店或更新流程。
- [x] 3.2 實作 `分享應用` 系統分享 Intent，分享內容固定為 `搭巴士前想快一點知道哪條路線合適？BusIsComing 可以保存常用路線，快速比較候車時間、票價和耗時。https://www.busiscoming.com`，無法分享時 Toast `暫時無法分享應用`。
- [x] 3.3 實作 `問題反饋` 郵件 Intent，收件人 `hezhenyu966@gmail.com`、主題 `BusIsComing 問題反饋`，正文預填 App 版本、Android 版本、設備型號與問題描述提示，無法打開時 Toast `暫時無法開啟問題反饋`。
- [x] 3.4 實作 `隱私政策` 外部 URL 打開 `https://www.busiscoming.com/zh-hant/privacy/`，無法打開時 Toast `暫時無法開啟隱私政策`。
- [x] 3.5 實作關於我們頁官網入口外部 URL 打開 `https://www.busiscoming.com`，無法打開時 Toast `暫時無法開啟網站`。
- [x] 3.6 將官網 URL、隱私政策 URL、分享文案、反饋郵箱、Toast 文案集中在資源或常量中，避免散落在多個點擊處理器。

## 4. 臨時查詢編輯入口

- [x] 4.1 更新 `activity_main.xml` 的 `temporaryQueryContextBar`：在現有 `保存` 入口旁新增 `編輯` 入口，兩者保持獨立觸控區，路徑文字在空間不足時單行省略且不與入口重疊。
- [x] 4.2 擴展 `TemporaryRouteBottomSheet` 或等效表單入口，支援可選初始起點與終點；預填時輸入框顯示地點名稱，內部 selected place 狀態有效，且可直接點 `使用此路線查詢`。
- [x] 4.3 保持普通臨時查詢入口的既有行為：從首次引導頁或普通入口打開時不帶入舊上下文，仍可按既有規則自動填入目前位置起點。
- [x] 4.4 調整預填編輯模式的目前位置行為：有預填起點時不被自動目前位置覆蓋，但起點輸入框內定位按鈕仍可手動改用目前位置。
- [x] 4.5 在 `MainActivity` 為 `編輯` 入口接線：僅當 `currentQueryContext` 是臨時查詢時打開預填彈層；通過校驗並點 `使用此路線查詢` 後以修改後起終點發起新臨時查詢並替換上下文。
- [x] 4.6 確認預填編輯彈層被返回、關閉或點擊外部 dismissed 時，不清空原臨時查詢結果、排序狀態、更新時間或上下文，也不自動保存為常用路線。
- [x] 4.7 確認 `保存` 入口仍沿用既有保存臨時查詢為常用路線流程，且 `編輯` 不更新任何已保存常用路線的使用次數或最近使用時間。

## 5. 測試與回歸

- [x] 5.1 為外部 Intent 建立可測試的 builder/helper 或等效薄封裝，補充單元測試驗證分享內容、反饋收件人、主題、正文診斷資訊、官網 URL 與隱私政策 URL。
- [x] 5.2 補充主頁 UI contract 或 instrumentation 測試，覆蓋有保存路線時顯示 `乘車碼`、`設定`、`全部` 與 `管理路線` 圖示，且頂部不再顯示 `管理路線` 文字按鈕。
- [x] 5.3 補充首次引導頁 UI contract 或 instrumentation 測試，覆蓋沒有保存路線時仍顯示 `設定`，且不顯示 `全部` 或 `管理路線` 入口。
- [x] 5.4 補充臨時查詢上下文條測試，覆蓋臨時結果顯示 `保存` 與 `編輯`、點擊 `編輯` 打開預填彈層、點擊 `保存` 仍打開保存流程。
- [x] 5.5 補充臨時查詢預填編輯測試，覆蓋直接再次查詢、修改起終點後查詢、關閉不提交保留原結果、普通入口不帶入舊上下文、自動目前位置不覆蓋預填起點。
- [x] 5.6 補充設定頁測試，驗證頁面標題、App 名稱、版本、三組分組、入口順序、暫不支援 Toast 與返回主頁行為。
- [x] 5.7 補充關於我們頁測試，驗證 App 名稱、版本、簡介、官網入口、返回設定與官網打開失敗 Toast。
- [x] 5.8 回歸既有路線管理入口流程，確認點擊常用路線標題行的管理圖示仍打開既有路線管理頁，新增、編輯、複製、刪除流程未改變。

## 6. 視覺與最終驗證

- [x] 6.1 在模擬器或實機檢查首頁普通狀態、首次引導頁、臨時查詢上下文、設定頁、關於我們頁、常用路線標題行入口，確認觸控區、間距、圖示清晰度、返回行為與字體放大下不重疊。
- [x] 6.2 在模擬器或實機檢查臨時查詢編輯流程：臨時結果點 `編輯` 預填起終點、修改後再次查詢、返回不提交保留原結果、`保存` 流程不受影響。
- [x] 6.3 驗證無可處理分享、郵件或瀏覽 Intent 的降級路徑，確認對應 Toast 文案正確且停留在當前頁。
- [x] 6.4 執行 `openspec validate add-settings-and-support-entrypoints --strict`，確保 proposal、design、specs、tasks 均通過 OpenSpec 驗證。
- [x] 6.5 執行 `./gradlew testDebugUnitTest`，確認新增與既有 JVM 測試通過。
- [x] 6.6 執行 `./gradlew build`，確認編譯、單元測試、lint 與 assemble 通過。
- [x] 6.7 檢查 `git status --short` 與變更範圍；若為 `/opsx-apply` 實作完成階段，依專案規則提交本次改動。
