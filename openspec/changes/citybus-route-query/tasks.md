## 1. 依赖与数据模型

- [x] 1.1 在 Gradle version catalog 中新增 jsoup 依赖，并在 app 模块引入。
- [x] 1.2 扩展 `BusRouteOption`，增加预计路线总耗时、预计汽车到站时间和中转次数字段。
- [x] 1.3 调整 `MockBusRouteRepository` 的测试数据，使其符合新的四列表格模型。
- [x] 1.4 更新现有受影响单元测试，确保旧字段 `waitMinutes` 不再被引用。

## 2. Citybus 路线查询 HTML 解析

- [x] 2.1 新增 `CitybusRouteParser`，使用 jsoup 定位 `id` trim 后等于 `routelist2` 的容器。
- [x] 2.2 实现基于 `aria-label` 的候选路线解析，提取多段路线号、每段价格和预计总耗时。
- [x] 2.3 实现 table 文本兜底解析，覆盖 `aria-label` 缺失或不完整的候选路线。
- [x] 2.4 计算路线展示名、总价格、预计路线总耗时、预计汽车到站时间和中转次数。
- [x] 2.5 对无有效候选返回空列表，对缺少 `routelist2` 或无法识别格式抛出解析错误。
- [x] 2.6 增加 parser 正常响应测试，使用用户提供 HTML 样例验证解析出 14 条结果。
- [x] 2.7 增加 parser 多段路线测试，验证 `82X → 102`、总价 `20.4`、总耗时 `34`、到站时间 `34`、中转次数 `1`。
- [x] 2.8 增加 parser 单段路线测试，验证 `8X`、总价 `8.1`、总耗时 `45`、中转次数 `0`。
- [x] 2.9 增加 parser 边界测试，覆盖 `routelist2` id 带尾随空格、无有效候选、缺少 `routelist2` 和错误格式。

## 3. Citybus 路线查询仓库

- [x] 3.1 新增 `CitybusBusRouteRepository` 实现 `BusRouteRepository`。
- [x] 3.2 构造查询 URL，包含 `slat`、`slon`、`elat`、`elon`、`t`、`leg=2`、`m1=T`、`l=0`。
- [x] 3.3 使用 `Asia/Hong_Kong` 当前时间生成 `yyyy-MM-dd HH:mm` 格式的 `t` 参数，并进行 URL 编码。
- [x] 3.4 设置用户 cURL 中的 headers 和 Cookie，保持与 Citybus Web 请求一致。
- [x] 3.5 处理 HTTP 非 2xx、网络异常和 parser 异常，向调用方暴露查询失败。
- [x] 3.6 增加仓库 URL 构造测试，验证经纬度、固定参数和香港时间格式。
- [x] 3.7 增加仓库 headers 测试，验证关键 headers 和 Cookie 被设置。

## 4. 主界面查询流程

- [x] 4.1 将 `MainActivity` 使用的默认查询仓库从 Mock 切换为 `CitybusBusRouteRepository`。
- [x] 4.2 将查询流程改为后台线程执行，避免在 UI 线程发起网络请求。
- [x] 4.3 查询中禁用查询按钮并展示查询中状态，查询结束后恢复按钮。
- [x] 4.4 查询成功有结果时展示表格并清除失败/空状态。
- [x] 4.5 查询成功无结果时展示“暂无可用巴士路线”。
- [x] 4.6 查询失败时展示“路线查询失败，请稍后重试”，并清空旧结果。
- [x] 4.7 路线切换或配置刷新时清空查询状态和旧结果。

## 5. 四列结果表格与排序

- [x] 5.1 更新 `activity_main.xml` 表头为 `路线`、`价格(HKD)`、`总耗时(分钟)`、`预计汽车到站时间(分钟)`。
- [x] 5.2 更新 `item_bus_route.xml` 为四列行布局，保证表头和行列宽一致。
- [x] 5.3 更新 `BusRouteAdapter`，价格、总耗时和预计汽车到站时间单元格只显示数值。
- [x] 5.4 扩展 `SortField`，新增路线、中转次数、总耗时和预计汽车到站时间相关排序字段。
- [x] 5.5 更新 `BusRouteSorter`，路线列按中转次数排序，价格、总耗时、预计到站时间按数值排序。
- [x] 5.6 更新表头点击逻辑，支持路线、价格、总耗时、预计汽车到站时间四列升序/降序切换。
- [x] 5.7 更新排序方向展示，当前排序表头显示 `↑` 或 `↓`。
- [x] 5.8 增加或更新排序单元测试，覆盖四列升序和降序。

## 6. 验证与回归

- [x] 6.1 执行 `./gradlew build`，确认编译、单元测试、lint 和 assemble 通过。
- [x] 6.2 如依赖下载受网络或沙箱限制，记录失败原因并在可用网络下重试。
- [x] 6.3 在模拟器或真机验证主界面选择已保存路线后能发起真实查询并展示四列表格。
- [x] 6.4 验证查询结果包含单段和多段路线时，路线号使用 `→` 连接并且价格为多段累计值。
- [x] 6.5 验证路线、价格、总耗时、预计汽车到站时间四列表头均可升序/降序排序。
- [x] 6.6 验证查询中按钮禁用，查询失败显示“路线查询失败，请稍后重试”，无结果显示“暂无可用巴士路线”。
- [x] 6.7 验证四列表头和数据在 Pixel 8 模拟器宽度下不重叠、不截断关键内容。

## 验证记录

- `./gradlew testDebugUnitTest` 通过，覆盖 Citybus HTML 解析、URL/headers 构造、四列排序和 Mock 仓库回归。
- `./gradlew build` 通过，覆盖编译、单元测试、lint 和 assemble。
- Pixel 8 模拟器安装并启动 app 后，样例路线 `样例真实查询：漁灣邨漁進樓 -> 循道衛理聯合教會北角衛理堂` 能发起真实 Citybus 查询，并展示四列表格。
- 实时查询首屏结果包含 `8X / 8.1 / 36 / 36`、`106 / 12.9 / 37 / 37`、`85 / 5.2 / 44 / 44`、`8H / 7.0 / 61 / 61`。
- 价格、总耗时、预计汽车到站时间、路线四个表头均已通过 ADB UI 树验证升序/降序箭头和首屏顺序变化。
- 用户提供 HTML 样例的 parser 单元测试验证多段路线 `82X → 102`、累计价格 `20.4`、总耗时 `34`、预计到站时间 `34` 和中转次数 `1`。
- 使用一条已有无可用结果路线验证空状态显示 `暂无可用巴士路线`；在飞行模式下触发查询失败并验证页面显示 `路线查询失败，请稍后重试`。
