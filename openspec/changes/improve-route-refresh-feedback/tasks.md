## 1. 結果列表浮層佈局

- [x] 1.1 在 `activity_main.xml` 的結果列表區加入可疊放 `SwipeRefreshLayout` 與固定狀態浮層的容器，保持排序、摘要與臨時查詢上下文在容器外。
- [x] 1.2 建立頂部中央的小型刷新浮層，包含進行中指示器與成功勾號，預設隱藏且不攔截列表觸控。
- [x] 1.3 補充最小必要的成功向量圖示、尺寸、顏色及「正在刷新／刷新完成」無障礙狀態描述，不新增第三方 UI 依賴。
- [x] 1.4 定義並保留結果列表原 padding，使浮層顯示時可增加約 44dp 頂部空間、結束後可準確恢復。

## 2. 刷新視覺狀態管理

- [x] 2.1 在 `MainActivity` 或同層輕量 helper 中實作 `Idle`、`Refreshing`、`Success` 刷新視覺狀態及集中渲染邏輯。
- [x] 2.2 保留下拉門檻前的原生 `SwipeRefreshLayout` 指示器，正式觸發後切換為固定浮層並保持舊結果可見。
- [x] 2.3 刷新期間沿用現有 `查詢中...` 按鈕狀態，停用查詢與再次下拉刷新，同時保持列表滾動、排序和卡片操作可用。
- [x] 2.4 將成功勾號收尾綁定目前 query generation，500ms 後恢復浮層、列表 padding、查詢按鈕和可刷新狀態。
- [x] 2.5 在切換路線、清空結果、開始新查詢、Activity 銷毀或 generation 失效時取消過期成功收尾。

## 3. 成功結果處理

- [x] 3.1 沿用 `onInitialRoutes` 作為刷新成功點，不等待 ETA 與站點預覽補全，也不修改 repository callback 協議。
- [x] 3.2 非空刷新結果返回後，立即按當時最新排序字段與方向替換結果、更新時間並將列表滾動到 position 0。
- [x] 3.3 非空刷新結果提交後將固定浮層切換為成功勾號，維持 500ms 後結束刷新狀態。
- [x] 3.4 空刷新結果返回後先在原結果上展示成功勾號 500ms，再切換到既有無結果狀態。
- [x] 3.5 保持已開啟詳情或 ETA 彈層所選路線，不因主列表替換、重排或回到頂部而主動關閉或切換。

## 4. 失敗與取消處理

- [x] 4.1 刷新前保存恢復失敗畫面所需的列表可見位置與偏移，並避免提前覆寫原更新時間或清空結果。
- [x] 4.2 刷新失敗時立即隱藏浮層、恢復原 padding 與滾動位置，並顯示 `刷新失敗，請稍後重試` Toast。
- [x] 4.3 確認失敗路徑保留原結果、排序、更新時間、查詢上下文及已開啟彈層，且不展示成功勾號或整頁失敗狀態。
- [x] 4.4 確認刷新被取消或查詢失效時不留下浮層、額外 padding、停用按鈕或延遲 Runnable。

## 5. 自動化測試

- [x] 5.1 擴充刷新 policy／狀態測試，覆蓋可刷新條件、重複提交阻止及成功確認期間保持停用。
- [x] 5.2 添加測試覆蓋非空成功結果立即替換、保留最新排序、更新時間並要求回到列表頂部。
- [x] 5.3 添加測試覆蓋空結果先展示 500ms 成功確認再切換無結果狀態。
- [x] 5.4 添加測試覆蓋失敗保留原結果、更新時間和滾動恢復資料，並結束刷新視覺狀態。
- [x] 5.5 添加測試覆蓋過期 generation 或生命週期取消後，成功收尾不得修改新狀態。
- [x] 5.6 運行相關單元測試並修正所有回歸。

## 6. 路線詳情巢狀滾動

- [x] 6.1 將 `RouteDetailBottomSheet` 的一般 `ScrollView` 替換為支援 `BottomSheetBehavior` 的 AndroidX `NestedScrollView` 或等效既有元件。
- [x] 6.2 配置內容容器的高度、fill viewport 與巢狀滾動屬性，保持既有初始彈層高度、長內容展開、拖動關閉及右上角關閉按鈕。
- [x] 6.3 確認內容未到頂時向下手勢優先滾動詳情內容，只有內容到頂後繼續下拉才交由 BottomSheet 收合或關閉。
- [x] 6.4 確認載入、失敗、重試、途經站折疊、分段時間線、API、解析器及快取行為不受改動。

## 7. 表單內嵌地點候選列表

- [x] 7.1 在共用 `PlaceInputController` 中建立內嵌候選列表管理，使用既有 `RecyclerView` 技術棧展示單行地點名稱並保留最多 100 條候選。
- [x] 7.2 更新新增、編輯及複製路線共用的表單佈局，在起點和終點輸入區域下方提供候選列表容器及可供外層自動滾動的結構。
- [x] 7.3 更新 `TemporaryRouteBottomSheet`，為起點和終點提供相同的內嵌候選列表容器，不使用獨立 PopupWindow 展示成功結果。
- [x] 7.4 實作同一時間只展示目前聚焦欄位候選的狀態切換，切換焦點時關閉另一欄位的候選列表。
- [x] 7.5 選擇一端地點後，如果另一端尚未選擇，保留輸入法並自動聚焦另一端；如果兩端均已選擇，不自動提交、收起輸入法或切換焦點。
- [x] 7.6 透過 `WindowInsetsCompat.Type.ime()` 計算輸入法上方可視區，將候選列表最大高度限制為約 40%，並確保至少 3 個完整候選項目可見和可點選。
- [x] 7.7 讓候選較多時由列表自身滾動；空間不足時自動滾動一般路線表單，確保目前輸入框及至少 3 個候選項目位於可視區。
- [x] 7.8 候選顯示時將臨時查詢底部彈層自動展開至接近全螢幕；候選關閉後恢復既有內容自然高度。
- [x] 7.9 實作點擊表單空白處或第一次按返回只關閉候選並保留輸入文字；候選已關閉後才沿用 Activity 或 BottomSheet 的既有返回行為。
- [x] 7.10 保持既有搜尋 debounce、generation、loading／無結果／失敗文案與位置、選擇失效、交換、校驗、保存及查詢行為。
- [x] 7.11 移除 `RouteEditActivity` 中已不再使用且與 `PlaceInputController` 重複的舊地點搜尋私有實作，不延伸重構其他流程。

## 8. 詳情與地點選擇自動化測試

- [x] 8.1 添加路線詳情 instrumentation 測試，覆蓋大量途經站展開後可向下瀏覽並再次向上返回頂部。
- [x] 8.2 添加手勢回歸測試，覆蓋詳情內容未到頂時向下滑動不收合 BottomSheet，以及內容到頂後仍可沿用下拉收合行為。
- [x] 8.3 添加共用地點候選狀態測試，覆蓋只顯示聚焦欄位、最多 100 條、過期結果忽略及搜尋狀態文案保持不變。
- [x] 8.4 添加 UI 測試，覆蓋輸入法顯示時至少 3 個候選完整可見、可點選、列表可獨立滾動及一般表單自動帶入可視區。
- [x] 8.5 添加 UI 測試，覆蓋選擇一端後自動聚焦未選擇的另一端，以及兩端均已選擇時不自動提交或收起輸入法。
- [x] 8.6 添加 UI 測試，覆蓋點擊空白處和第一次返回只關閉候選，第二次返回沿用頁面／BottomSheet 既有行為。
- [x] 8.7 運行相關單元測試與 instrumentation 測試並修正所有回歸。

## 9. 必要模擬器驗證與完整構建

- [x] 9.1 使用可用 Android 模擬器安裝並啟動 App；本 change 不得以「沒有設備」為由跳過模擬器驗證。
- [x] 9.2 在模擬器驗證常用路線與臨時查詢結果下拉手勢：門檻前顯示原生指示器，觸發後切換固定浮層。
- [x] 9.3 在刷新期間上下滾動列表，確認浮層始終位於可視區頂部、首卡不被遮擋，且排序、路線詳情與 ETA 操作仍可使用。
- [x] 9.4 在模擬器驗證非空成功路徑：新結果立即替換、更新時間更新、列表回到頂部、勾號約 500ms 後消失，且期間無法再次查詢或刷新。
- [x] 9.5 在模擬器驗證空結果成功路徑：勾號顯示約 500ms 後切換無結果狀態。
- [x] 9.6 在模擬器透過可復現的斷網或測試條件驗證失敗路徑：Toast 可見，原結果、更新時間及滾動位置保持不變。
- [x] 9.7 在模擬器驗證刷新期間打開的詳情／ETA 彈層不被主列表刷新主動關閉或切換。
- [x] 9.8 在模擬器打開具有大量途經站的路線詳情，展開站點後反覆向上、向下滑動，確認內容可返回頂部且到頂前彈層不收合。
- [x] 9.9 在模擬器分別驗證臨時查詢、新增、編輯及複製路線：輸入法顯示時至少 3 個候選完整可見、可點選且列表可獨立滾動。
- [x] 9.10 驗證一般路線表單會將目前輸入框和候選帶入可視區；臨時查詢候選顯示時接近全螢幕，關閉後恢復自然高度。
- [x] 9.11 驗證選擇一端後自動聚焦未選擇的另一端；兩端均已選擇時保持目前行為，不自動查詢或收起輸入法。
- [x] 9.12 驗證點擊空白處及第一次返回只關閉候選並保留文字，候選關閉後返回才退出頁面或關閉底部彈層。
- [x] 9.13 在不同常見螢幕尺寸與字體縮放下檢查刷新浮層、長詳情內容、輸入框及候選列表無遮擋、跳動和觸控問題。
- [x] 9.14 保留刷新成功／失敗、詳情滾動及輸入法候選的關鍵模擬器截圖或驗證記錄，供人工查驗。
- [x] 9.15 運行 `./gradlew build`，確認 Kotlin 編譯、單元測試、lint 與 debug／release assemble 全部通過。
