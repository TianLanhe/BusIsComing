## 1. 前置候選 UI 與規格協調

- [x] 1.1 確認 `improve-route-refresh-feedback` 的候選驗收修正已完成：正式候選卡、約 52dp 行高、4 至 6 個完整項目、`NestedScrollView` 及交換按鈕顯隱均已通過驗證。
- [x] 1.2 以本 change 的 `route-place-selection` 距離規格明確取代前一 change 中「候選項只顯示地點名稱／不顯示額外描述」的限制，不回退已確認的候選卡與高度設計。
- [x] 1.3 檢查兩個 change 的 apply／archive 順序，確認不會由後續同步覆蓋候選距離佈局或重新引入臨時表格行。

## 2. 定位依賴、權限與共用位置模型

- [x] 2.1 在 `gradle/libs.versions.toml` 和 `app/build.gradle.kts` 新增 Google Play Services Location 依賴，確認不引入 Kotlin Android 插件衝突。
- [x] 2.2 在 `app/src/main/AndroidManifest.xml` 宣告 `ACCESS_FINE_LOCATION` 和 `ACCESS_COARSE_LOCATION` 前台定位權限。
- [x] 2.3 新增不依賴 UI 的位置快照模型，包含經緯度、水平精度及可用於 30 秒新鮮度判斷的 monotonic 時間。
- [x] 2.4 在主頁定位流程中沿用 `ActivityCompat.requestPermissions(...)` 與 `onRequestPermissionsResult(...)` 風格，新增定位權限 request code 並避免影響既有通知權限流程。
- [x] 2.5 建立共用自動定位拒絕狀態：主頁、純新增路線與臨時查詢任一自動權限請求被拒絕後，其他自動入口不再彈權限視窗。
- [x] 2.6 明確區分權限入口：候選距離不單獨請求權限；純新增路線、臨時查詢與起點定位按鈕可為「使用目前位置作為起點」請求權限。
- [x] 2.7 處理系統定位關閉與設定恢復路徑：主動定位入口提示開啟系統定位，返回後最多自動重試一次。

## 3. 共用一次性定位協調

- [x] 3.1 建立共用定位協調元件，封裝 `FusedLocationProviderClient` 並提供可測試的成功、無權限、超時、空結果及不可用結果。
- [x] 3.2 實作最近 30 秒位置快照復用，使用 monotonic 時間判斷新鮮度，避免裝置時鐘調整影響。
- [x] 3.3 在無有效快照時以 `PRIORITY_HIGH_ACCURACY` 發起一次目前位置請求，並套用 3 秒超時。
- [x] 3.4 合併同時進行的位置需求，使主頁、路線表單或臨時查詢共用同一個底層定位請求。
- [x] 3.5 確保成功位置更新進程內快照，失敗不覆蓋仍有效快照，且不持久化位置資料。
- [x] 3.6 提供取消／generation 或等效生命週期保護，使已銷毀 Activity 或已關閉彈層不接收遲到 UI 更新。

## 4. 共用直線距離與格式化

- [x] 4.1 新增可 JVM 單測的 WGS84 Haversine 直線距離計算器，輸入兩組經緯度並輸出非負米數。
- [x] 4.2 將最近路線策略與候選距離接入同一計算器，避免 Android `Location` API 滲入純策略層。
- [x] 4.3 將通知監控現有 `WalkingTimeCalculator.straightLineDistanceMeters(...)` 改為委派共用計算器，保持既有步行時間結果語義。
- [x] 4.4 新增候選距離格式化器：先四捨五入整數米，低於 1000 顯示 `Xm`，達 1000 後顯示一位小數 `X.Xkm`。
- [x] 4.5 使用固定格式輸出小數點及緊貼單位，覆蓋 `368.4m -> 368m`、`999.4m -> 999m`、`999.6m -> 1.0km`、`1249.6m -> 1.3km`。
- [x] 4.6 為可見緊湊格式提供完整無障礙距離文案，例如「距離目前位置 368 米」。
- [x] 4.7 新增 `PlaceNameResolver` 或等效介面，本期 mock 固定返回 `目前位置附近`，並保留後續替換真實 reverse geocoding 的邊界。
- [x] 4.8 為目前位置起點流程建立組合超時：定位階段最多 3 秒，地點名稱解析階段最多 3 秒，整體最多 5 秒。

## 5. 最近路線選擇策略

- [x] 5.1 新增純邏輯 `NearbyRouteSelectionPolicy` 或等效策略，輸入位置快照與按常用排序排列的 `RouteConfig` 列表，輸出是否自動選中及 route id。
- [x] 5.2 實作 tie-breaker：距離相同或無法明確區分時保留傳入列表順序，不修改完整列表排序。
- [x] 5.3 實作精度規則：`accuracy <= 500m` 直接選最近；`accuracy > 500m` 時僅在最近起點比第二近起點至少近 `accuracy` 米時選中。
- [x] 5.4 覆蓋無效定位、缺少精度、少於 2 條路線、精度不足和明顯領先等 fallback 結果。

## 6. 主頁定位生命週期與降級

- [x] 6.1 在 `MainActivity` 接入共用定位協調元件，每次 Activity 建立後最多自動定位一次。
- [x] 6.2 常用路線少於 2 條時不請求權限、不定位、不顯示 `附近`，保持既有預設選中行為。
- [x] 6.3 定位進行中先按既有常用排序顯示預設選中路線，不新增 `定位中` UI。
- [x] 6.4 若定位結果返回前用戶已手動選擇路線，丟棄該次定位結果，不覆蓋用戶選擇。
- [x] 6.5 Google Play Services Location 不可用、定位失敗、定位超時或定位結果為空時，不回退 `LocationManager`，保持常用排序 fallback。
- [x] 6.6 從管理頁返回同一 Activity 時不重新定位，頁面重建時可復用仍在 30 秒內的位置快照。

## 7. 權限拒絕記錄與主頁 Toast 限頻

- [x] 7.1 使用 `SharedPreferences` 持久化用戶拒絕主頁自動定位權限請求的狀態。
- [x] 7.2 首次符合條件且無定位權限時請求精確和粗略定位；用戶拒絕後，後續未授權狀態不再自動彈權限請求。
- [x] 7.3 若用戶後續在系統設定重新授權，下一次主頁建立及後續路線表單恢復定位能力。
- [x] 7.4 將「僅本次允許」視為臨時授權；下次權限過期後仍可由主頁再次自動請求，不記為永久拒絕。
- [x] 7.5 實作三類主頁 fallback Toast 文案及同一 App 進程內每類最多一次的限頻：未允許定位、暫時無法取得目前位置、目前位置不夠精確。
- [x] 7.6 確認路線表單與臨時查詢的無權限、失敗及超時路徑不顯示 Toast、helper、placeholder 或錯誤卡。

## 8. 主頁快捷卡 UI

- [x] 8.1 在 `MainActivity` 增加臨時 `nearbySelectedRouteId` 類似狀態，表示本次自動定位選中的路線。
- [x] 8.2 在 `createRouteShortcutCard(...)` 中僅對目前選中且由定位選中的主頁快捷卡顯示 `附近` 標籤。
- [x] 8.3 定位成功且最近路線就是既有預設選中路線時，仍顯示 `附近` 標籤。
- [x] 8.4 用戶從快捷卡或完整列表手動選擇任一路線時清除 `附近` 標籤。
- [x] 8.5 保持完整 `全部` 路線列表既有樣式，不在列表項中顯示 `附近` 標籤。
- [x] 8.6 確認自動選中非原始 Top 3 路線時沿用現有臨時提升規則，不修改 `RouteConfigRepository` 排序和使用統計。

## 9. 候選距離項目與共用控制器

- [x] 9.1 在已驗收的正式候選項佈局中建立左右單行結構：左側 16sp 可省略地點名稱，右側不可換行的定位圖示與 13sp 次要色距離。
- [x] 9.2 新增約 14dp 的綠色線框定位 vector，保持地點名稱前沒有圓點或圖示，並將定位圖示標記為無障礙裝飾。
- [x] 9.3 擴充 `PlaceInputController`／候選 adapter，使其可接收可選位置快照並為每個 `Place` 計算 presentation 距離。
- [x] 9.4 候選先於位置返回時立即顯示名稱；位置稍後返回時只更新距離 presentation，不重新提交候選資料、不重排、不關閉列表。
- [x] 9.5 保持候選目前滾動位置、約 52dp 行高、最多 100 條及既有 4 至 6 個完整項目高度策略。
- [x] 9.6 確保長地點名稱尾端省略且距離完整顯示；整個候選行維持既有點擊、按壓及無障礙操作。
- [x] 9.7 選定候選後輸入框只顯示地點名稱，不把距離加入 `Place`、Intent extra、路線配置或資料庫。
- [x] 9.8 保持 Citybus 原始候選順序，不依距離排序、過濾或自動選擇。

## 10. 路線新增／編輯／複製頁接入

- [x] 10.1 在 `RouteEditActivity` 明確區分純新增、編輯與複製模式：純新增可自動使用目前位置填起點；編輯與複製不自動覆蓋既有或預填起點。
- [x] 10.2 在起點 `TextInputLayout` 內加入定位 end icon／等效內嵌按鈕，確保至少 48dp 觸控目標、`使用我的位置` 無障礙描述，且不壓縮起點輸入框寬度。
- [x] 10.3 純新增頁面建立後，若起點尚未預填且用戶尚未操作，啟動一次目前位置起點流程；成功後調用 `setSelectedPlace(Place("目前位置附近", gpsLat, gpsLon))`，不自動聚焦終點、不彈鍵盤、不查詢。
- [x] 10.4 起點定位按鈕點擊後可請求定位權限；定位進行中禁用重複點擊但保持輸入可編輯，成功後才替換起點，失敗時保留原起點或原輸入。
- [x] 10.5 若用戶在自動或手動定位結果返回前編輯、清空或選擇其他起點，丟棄該次遲到結果，不覆蓋用戶最新操作。
- [x] 10.6 將同一位置快照提供給起點和終點 `PlaceInputController` 顯示候選距離，避免兩個輸入框各自發起定位；候選距離本身不單獨彈權限。
- [x] 10.7 位置返回前後保持目前焦點、輸入文字、候選可見性及列表滾動位置。
- [x] 10.8 Activity 銷毀後忽略遲到結果，且頁面重建可復用進行中請求或 30 秒內快照，不重複彈權限。
- [x] 10.9 驗證新增、編輯及複製三種模式的目前位置起點、候選距離、交換、校驗及保存流程不受影響。

## 11. 臨時查詢接入

- [x] 11.1 在每次 `TemporaryRouteBottomSheet` 會話開啟時，若起點尚未由用戶操作決定，啟動一次目前位置起點流程並優先復用 30 秒內快照。
- [x] 11.2 在臨時查詢起點輸入框內加入定位 end icon／等效內嵌按鈕，行為與路線表單起點定位按鈕一致，終點不新增定位按鈕。
- [x] 11.3 臨時查詢自動定位成功後調用 `setSelectedPlace(Place("目前位置附近", gpsLat, gpsLon))`，不自動聚焦終點、不自動查詢。
- [x] 11.4 臨時查詢自動定位失敗時起點保持空白並提示手動選擇；手動按鈕失敗時保留原起點或原輸入。
- [x] 11.5 將同一位置快照提供給臨時起點和終點控制器，候選距離更新不改變 BottomSheet 展開狀態或列表滾動位置。
- [x] 11.6 彈層關閉後忽略遲到位置結果，不重新開啟彈層或候選。
- [x] 11.7 驗證臨時查詢、保存為常用、交換及輸入校驗流程不受目前位置起點或距離 presentation 影響。

## 12. 單元測試與回歸

- [x] 12.1 新增定位協調測試，覆蓋無權限、30 秒快照命中、快照過期、新定位成功、3 秒超時、空結果、不可用及同時請求共用。
- [x] 12.2 新增距離計算測試，覆蓋已知香港座標、相同座標、無效座標防護及通知監控既有結果回歸。
- [x] 12.3 新增格式化邊界測試，覆蓋 `368m`、`999m`、`1.0km`、`1.3km` 及固定小數點格式。
- [x] 12.4 新增最近路線策略測試，覆蓋少於 2 條路線、精度小於等於 500 米、粗略位置明顯領先、精度不足及 tie-breaker。
- [x] 12.5 新增主頁選擇狀態測試，確認自動選中不記錄使用統計、不改完整列表排序，且非 Top 3 路線臨時提升。
- [x] 12.6 新增手動選擇與過期定位結果測試，確認手動選擇後丟棄延遲定位結果並清除 `附近`。
- [x] 12.7 新增權限拒絕與 Toast 限頻測試，確認主頁三類 fallback 各自限頻，且候選入口完全靜默。
- [x] 12.8 新增候選 presentation 測試，確認位置稍後返回只補充距離、不重排、選定後輸入框只保留名稱。
- [x] 12.9 新增 mock `PlaceNameResolver` 測試，確認 GPS 成功時固定返回 `目前位置附近` 並保留原始緯度、經度建立 `Place`。
- [x] 12.10 新增目前位置起點狀態測試，覆蓋純新增與臨時查詢自動成功、編輯與複製不自動覆蓋、無預填條件不啟動。
- [x] 12.11 新增起點定位按鈕測試，覆蓋成功後替換、失敗保留原起點、定位中禁止重複點擊但允許輸入。
- [x] 12.12 新增用戶操作競態測試，確認定位或名稱解析結果返回前用戶編輯／清空／選擇起點時，遲到結果不覆蓋。
- [x] 12.13 新增目前位置起點超時測試，覆蓋定位 3 秒、名稱解析 3 秒與整體 5 秒超時。
- [x] 12.14 新增共享自動拒絕狀態測試，確認主頁、純新增路線與臨時查詢任一自動權限拒絕後，其他自動入口不再彈窗，但手動定位按鈕仍可走恢復流程。

## 13. Instrumentation 與模擬器驗證

- [x] 13.1 補充候選 UI instrumentation，直接斷言定位圖示位置、13sp 距離、16sp 名稱、約 52dp 行高、單行省略及完整距離。
- [x] 13.2 驗證位置返回前候選名稱立即可見，返回後原位補距離且列表不關閉、不閃爍、不改變滾動位置。
- [x] 13.3 驗證無權限、粗略權限、精確權限、定位關閉、超時及空結果下候選仍可搜尋與選擇。
- [x] 13.4 在新增、編輯、複製和臨時查詢流程驗證距離顯示、交換、選擇、保存及返回行為。
- [x] 13.5 在 1080 × 2400、字體縮放 1.0 驗證 4 至 6 個完整候選；在 1080 × 1920、字體縮放 1.15 驗證至少 3 個，距離不得增加行高。
- [x] 13.6 驗證窄屏、長地點名稱及大字體下名稱正確省略、距離完整、TalkBack 語義清楚。
- [x] 13.7 在模擬器設定不同 GPS 位置，核對候選米／公里格式及主頁最近路線精度規則。
- [x] 13.8 保留主頁 `附近`、候選米距離、候選公里距離、無權限純名稱及長名稱省略的關鍵截圖。
- [x] 13.9 驗證純新增路線與臨時查詢在允許定位後自動填入起點 `目前位置附近`，且不自動聚焦終點、不彈鍵盤、不查詢。
- [x] 13.10 驗證編輯與複製路線不自動覆蓋既有起點，點擊起點定位按鈕後才可改用目前位置。
- [x] 13.11 驗證起點定位按鈕位於輸入框內，不壓縮輸入框寬度，終點沒有定位按鈕，候選列表沒有固定目前位置項。
- [x] 13.12 驗證定位失敗、權限拒絕、系統定位關閉和超時時，自動流程與手動按鈕分別呈現正確 helper／Toast，且既有輸入不丟失。

## 14. 完整驗證與收尾

- [x] 14.1 運行針對性 JVM 單元測試及完整 instrumentation 測試，修正所有回歸。
- [x] 14.2 運行 `./gradlew build`，確認 Kotlin 編譯、單元測試、lint 及 debug／release assemble 全部通過。
- [x] 14.3 運行 `openspec validate select-nearest-route-by-location --strict`，確認 proposal、design、兩個 capability specs 及 tasks 一致。
- [x] 14.4 檢查 `git status --short` 和變更範圍，確認未提交構建產物、位置資料、無關改動或 `.superpowers` 預覽檔。
