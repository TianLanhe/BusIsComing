## 1. 共享視覺基礎

- [x] 1.1 新增淡綠到近白的頁面根背景資源，套用到主頁、路線管理頁與路線新增/編輯/複製頁；主頁和管理頁卡片、輸入框、底部彈層與對話框保持實色表面，新增/編輯/複製頁表單功能區使用淡色漸變。
- [x] 1.2 新增或更新首次引導標題、示例標籤、主次操作按鈕等繁體中文文案。
- [x] 1.3 更新佈局或文案測試，覆蓋主頁不再出現「巴士查詢」、首次無路線狀態隱藏「管理路線」、首次引導文案與操作存在。

## 2. 結果卡片重用

- [x] 2.1 從 `BusRouteAdapter.BusRouteViewHolder` 抽出可重用的結果卡片綁定器或等價結構。
- [x] 2.2 保持真實列表結果卡原有行為不變，包括路線點擊、ETA 點擊、監控按鈕、不可監控狀態與大字體顯示。
- [x] 2.3 建立固定的 `118` 示例路線 UI model/factory，避免觸發網路、儲存或查詢副作用。
- [x] 2.4 補充單元或合約測試，確認示例卡使用同一套格式化與綁定預期。

## 3. 首次主頁體驗

- [x] 3.1 將舊 `emptyRouteState` 調整為中上居中的首次引導版面：低權重「乘車碼」、主標題、`示例預覽` 結果卡與 CTA 按鈕組。
- [x] 3.2 將 `新增常用路線` 連到路線新增頁，將 `直接查詢一次` 連到臨時查詢底部彈層。
- [x] 3.3 更新 `MainActivity` 狀態邏輯：僅在無常用路線且沒有臨時查詢上下文/結果時顯示首次引導；臨時查詢結果、失敗或查詢中狀態隱藏首次引導；關閉底部彈層且未查詢時回到首次引導。
- [x] 3.4 為首次引導加入一次性的克制進場動畫，尊重系統動畫設定且不阻塞點擊。

## 4. 正常主頁兼容

- [x] 4.1 保持已有常用路線時「乘車碼」與「管理路線」的頂部位置與既有按鈕樣式不變。
- [x] 4.2 讓結果區與列表背景透出頁面根背景，同時保持結果卡、排序 chip、狀態卡等內容表面為實色。
- [x] 4.3 確認既有常用路線選擇、查詢、排序、刷新與臨時查詢保存為常用路線流程不變。

## 5. 管理與編輯頁 polish

- [x] 5.1 為路線管理頁套用根背景，並輕量調整標題、副標題、間距與列表卡片感，不改變新增、編輯、複製、刪除流程。
- [x] 5.2 為路線新增/編輯/複製頁套用根背景、淡色漸變表單功能區與接近原狀的垂直居中位置，不改變校驗、地點搜尋、交換起終點與保存流程。
- [x] 5.3 確認路線編輯頁候選列表、Google attribution、交換按鈕、鍵盤與滾動行為仍可用。

## 6. 驗證與提交

- [x] 6.1 執行或更新 formatter、binder 與主頁狀態策略相關 JVM 單元測試。
- [x] 6.2 執行 `./gradlew testDebugUnitTest`。
- [x] 6.3 執行 `./gradlew build`。
- [x] 6.4 執行 `openspec validate polish-first-run-homepage --strict`。
- [x] 6.5 在已啟動模擬器上驗證首次主頁、已有路線主頁、無常用路線的臨時查詢結果、路線管理頁與新增/編輯頁。
- [x] 6.6 檢查截圖或 UI tree，確認文字不重疊、觸控目標足夠、淡綠背景與實色卡片對比穩定。
- [x] 6.7 檢查 `git status --short` 與 staged scope。
- [x] 6.8 提交本次實作與 OpenSpec 更新。
