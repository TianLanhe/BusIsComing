## 1. 卡片頂部布局

- [x] 1.1 調整 `item_bus_route.xml` 頂部區域，明確保留右側候車資訊區固定寬度，不讓左側文本侵入。
- [x] 1.2 保持 `busWaitArea` 現有展示寬度、候車主文案、下一班摘要和鈴鐺 `48dp` 觸控區不變。
- [x] 1.3 將 `busStopPreviewText` 改為固定左側可用寬度內單行展示，使用 `ellipsize=end` 且 `maxLines=1` 或等效策略。
- [x] 1.4 調整左側路線號與站點預覽的垂直排布，使左側整體高度與右側候車區高度一致。
- [x] 1.5 確認分隔線和底部價格、耗時、步行距離不因站點文案長度而下移。

## 2. 綁定與降級策略

- [x] 2.1 更新 `BusRouteAdapter` 或相關 binding，移除依下一班可見性放寬站點預覽到右側區域的策略。
- [x] 2.2 確認沒有下一班摘要時仍保留完整右側候車區寬度，站點預覽不額外延伸。
- [x] 2.3 確認大字體或窄屏下站點預覽仍保持單行省略，不使用兩行降級。
- [x] 2.4 保留卡片點擊、候車區點擊和鈴鐺點擊既有行為。

## 3. 測試

- [x] 3.1 更新 `RouteCardLayoutContractTest`，確認站點預覽 `maxLines=1`、尾部省略且不再允許兩行展示。
- [x] 3.2 更新 layout contract，確認右側候車資訊區固定寬度與鈴鐺 `48dp` 觸控區保持不變。
- [x] 3.3 新增或更新 contract 測試，覆蓋長文案 `興華邨豐興樓 → 海底隧道巴士轉乘站` 不得侵入候車區且必須單行省略。
- [x] 3.4 保留或更新點擊事件 contract，確認卡片、候車區和鈴鐺點擊互不干擾。

## 4. 驗證

- [x] 4.1 運行 `./gradlew testDebugUnitTest`。
- [x] 4.2 運行 `./gradlew build`。
- [x] 4.3 運行 `openspec validate tighten-route-card-left-column-layout --strict`。
- [x] 4.4 使用模擬器或真機截圖驗證長站點名卡片：站點預覽單行省略，不侵入右側候車區，不換行撐高頂部。
- [x] 4.5 使用模擬器或真機驗證正常字體、大字體和無下一班摘要場景下，右側候車區寬度穩定且卡片高度不跳動。
