## 1. 結果摘要

- [x] 1.1 在主頁結果區新增摘要容器，位於排序控件和結果清單上方，支援左側 `共 X 條路線，Y 條直達`、右側 `更新時間：HH:MM:SS`
- [x] 1.2 在 `MainActivity` 查詢成功展示初始結果時記錄本次更新時間，並基於完整結果數與 `transferCount == 0` 計算直達數
- [x] 1.3 在無結果、查詢中、查詢失敗、切換路線清空結果時隱藏或重置結果摘要

## 2. 路線卡片佈局

- [x] 2.1 調整 `item_bus_route.xml`：第一行左側為簡潔文字路線，右側為等候狀態，不使用號牌樣式
- [x] 2.2 在卡片中加入或保留輕分隔線，將價格移到分隔線下方的信息區
- [x] 2.3 底部信息區按 `價格 · 耗時 X 分鐘 · 步行 X 米` 展示，不展示轉乘次數
- [x] 2.4 為長路線文字、窄屏和字體放大設置不重疊策略，確保右側等候狀態仍可讀

## 3. 文案與狀態顏色

- [x] 3.1 將所有面向用戶的 `總耗時` 改為 `耗時`，包含卡片、排序控件和測試期望
- [x] 3.2 將可用 ETA 文案改為 `等候 X 分鐘`，並使用主綠色
- [x] 3.3 將無可用車輛文案改為 `暫無車輛`，並使用灰色，不能使用橙色
- [x] 3.4 保留查詢中等候狀態的可理解文案，避免與 `等候 X 分鐘` 或 `暫無車輛` 混淆

## 4. 排序與行為保持

- [x] 4.1 保留現有排序字段和排序算法，默認仍按耗時升序展示
- [x] 4.2 將耗時排序 chip 文案改為 `耗時`，選中時展示升降序方向
- [x] 4.3 確認價格排序、候車排序、步行排序和路線排序行為不因 UI 調整而變更

## 5. 測試與驗證

- [x] 5.1 補充或更新單元測試，覆蓋 `等候 X 分鐘`、`暫無車輛`、`耗時` 和價格展示位置對應的綁定文案
- [x] 5.2 補充或更新主頁邏輯測試，覆蓋摘要中的路線總數、直達數和更新時間顯示/隱藏狀態
- [x] 5.3 運行 `./gradlew build`
- [x] 5.4 使用模擬器或截圖檢查主頁結果卡片，確認價格在底部信息區、等候狀態在右側、`暫無車輛` 為灰色、卡片沒有文字重疊
