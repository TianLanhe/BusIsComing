## 1. 卡片 XML 布局調整

- [x] 1.1 調整 `item_bus_route.xml` 頂部內容區高度，使路線/站點預覽與候車區在更緊湊的固定高度內展示。
- [x] 1.2 將左側路線文字與站點預覽包成獨立垂直文字欄，整體垂直居中，並保留右側候車區避讓空間。
- [x] 1.3 將右側 `busWaitArea` 改為水平兩欄：左欄垂直展示 `busArrivalText` 和 `busNextArrivalText`，右欄展示 `busMonitorButton`。
- [x] 1.4 保留鈴鐺無外框樣式、既有圖示、`48dp` 觸控區和內容描述，不改變監控點擊行為。
- [x] 1.5 將 ETA 下拉點擊綁定從整個 `busWaitArea` 收斂到候車文字欄，候車區空白不觸發下拉。
- [x] 1.6 收斂卡片頂部右側視覺 padding，使鈴鐺視覺右邊距接近左側文字起點，底部分隔線和資訊區仍保留右邊距。
- [x] 1.7 確認站點預覽保持單行尾部省略，長站名不侵入右側候車文字或鈴鐺欄。

## 2. 驗證

- [x] 2.1 執行 `./gradlew build`，確認 XML、資源和既有測試通過。
- [x] 2.2 安裝 debug APK 到模擬器，查詢路線結果並截取包含站點預覽、下一班摘要和鈴鐺的卡片畫面。
- [x] 2.3 使用截圖或 UI tree 驗證右側候車文字與鈴鐺分欄展示，主副標題間距未被鈴鐺撐大。
- [x] 2.4 使用長站名卡片驗證站點預覽使用左側可用寬度、保持單行、超出省略且不重疊。
- [x] 2.5 對比卡片頂部和底部視覺留白，確認頂部沒有明顯大於底部的空白。
- [x] 2.6 使用 UI tree 或手動點擊驗證候車文字欄可打開 ETA 下拉，候車區左側空白不打開 ETA 下拉。
- [x] 2.7 使用截圖或 UI tree 驗證右側鈴鐺視覺邊距收斂後，底部資訊區仍與左側邊距一致。

## 3. 收尾

- [x] 3.1 更新本 change 的任務勾選狀態。
- [x] 3.2 執行 `openspec validate tighten-route-card-layout --strict`。
- [x] 3.3 檢查 `git status --short`，確認變更範圍只包含本 change 和預期布局文件。
