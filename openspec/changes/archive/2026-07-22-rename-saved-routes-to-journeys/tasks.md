## 1. 盤點與分類現有文案

- [x] 1.1 使用 `rg` 盤點 `app/src/main/res/values*/strings.xml`、XML、Kotlin、測試及現行文件中的「常用路線／常用路线／route／journey」相關文字，逐項區分已保存行程、查詢路線、乘車段、用戶原文及第三方原文。
- [x] 1.2 建立必須改為行程的畫面清單，至少覆蓋常用首頁、首次引導、新增／編輯／複製／刪除／管理、搜尋保存、設定、匯入匯出、Dialog、Toast 和無障礙描述，並確認每項均來自 Android resources。
- [x] 1.3 建立必須保留路線的負向清單，至少覆蓋查詢操作、載入／空／失敗狀態、結果摘要、路線卡、路線號、ETA、路線詳情、站點、換乘與通知欄監控。
- [x] 1.4 盤點直接斷言受影響 runtime 文案的 unit／instrumentation 測試，確認後續只更新期望文字而不改變測試流程、資料和行為斷言。

## 2. 更新三語 Runtime 文案

- [x] 2.1 更新繁體、簡體和英文的常用頁、首次引導、完整列表及臨時查詢保存文案，分別使用「常用行程／行程」「常用行程／行程」和 `Regular journey / journey`，同時保留查詢結果的路線語義。
- [x] 2.2 更新三語新增、編輯、複製、刪除及管理畫面的標題、行程名稱欄位、按鈕、確認、空狀態、驗證錯誤、成功／失敗提示和 content description，保持原有 resource key、導航及保存行為。
- [x] 2.3 更新三語設定、行程資料、匯入匯出、預覽、合併、取代、數量、私隱警告和結果文案，保留 `.bicroutes` 副檔名、檔名、JSON schema 及所有傳輸行為。
- [x] 2.4 審校同時包含行程與路線的完整句子，明確表達「選擇／保存行程後查詢路線」，避免出現「查詢行程」或暗示行程綁定某條路線的文案。
- [x] 2.5 重新檢查查詢、結果、路線詳情、ETA、乘車段、監控和第三方動態原文，確認正確的「路線／路线／Route」未被誤改，且用戶自訂名稱與已保存地點保持原樣。
- [x] 2.6 檢查最終 diff，確認未重命名 string resource key、Kotlin 符號或檔名，亦未修改 SQLite、`.bicroutes`、外部請求、解析、cache、callback 或語言 snapshot。

## 3. 沉澱項目術語文件

- [x] 3.1 更新 `docs/localization-guidelines.md` 的權威三語術語表與文字規則，定義行程、路線、乘車段、用戶原文和第三方原文的邊界。
- [x] 3.2 更新 `AGENTS.md`，加入簡潔的長期概念規則、內部 `RouteConfig` 等歷史命名兼容例外，並指向本地化指南。
- [x] 3.3 更新 `README.md` 及描述目前產品行為的必要現行文件，以行程描述保存配置、以路線描述查詢結果，且不批量改寫已完成或歷史 OpenSpec artifacts。
- [x] 3.4 更新 `docs/localization-validation-matrix.md` 的相關畫面與驗收用詞，保留三語、明暗、窄屏、大字體及動態原文的既有驗證範圍。

## 4. 自動化測試與語義回歸

- [x] 4.1 更新直接斷言舊用戶文案的 unit／instrumentation 測試期望，補齊常用首頁、行程管理、搜尋保存或匯入匯出中缺少的關鍵術語斷言，但不改變業務測試邏輯。
- [x] 4.2 運行受影響的 unit 測試及可執行的 instrumentation 測試，確認新增／編輯／複製／刪除／保存／匯入匯出流程與既有資料行為保持不變。
- [x] 4.3 使用語義掃描確認三語 resources 中的行程類舊詞已清理，並抽查「查詢路線」「路線詳情」「路線號」「Route details」等負向保留項仍正確。
- [x] 4.4 驗證既有 SQLite 資料和版本 1 `.bicroutes` fixture／測試仍可原樣讀取，且本 change 沒有新增 migration、協議轉換或用戶資料改寫。

## 5. 構建與人工驗收

- [x] 5.1 運行 `./gradlew build`，修正任何由資源文案或測試期望引起的編譯、unit test、lint 或 assemble 問題。
- [x] 5.2 使用 `adb devices` 檢查可用設備；在可用模擬器／實機上驗證繁體、簡體、英文 × 淺／深色的常用首頁、行程管理、搜尋保存和匯入匯出主要流程。
- [x] 5.3 在約 360dp 及 font scale 1.0／1.3／2.0 下檢查標題、按鈕、Dialog、錯誤提示、長摘要和 content description，確認核心行程／路線文字不裁切、可滾動且焦點順序不變；無設備或未完成項須在交付中明確記錄。
- [x] 5.4 重新核對 `journey-route-terminology` 的所有 scenarios、更新本 tasks 勾選狀態，檢查 `git status --short` 及提交範圍，並依 `/opsx-apply` 項目規則自動提交已驗證的實作。
