## 1. 共享视觉资源

- [x] 1.1 对照 `docs/ui-style-guide.md` 梳理当前颜色、圆角、卡片、状态区和按钮资源，确认本次继续使用 XML + AppCompat + Material Components
- [x] 1.2 新增或调整结果卡片、状态卡、路线管理卡片、排序控件和交换图标所需的 drawable/color/dimen/style 资源
- [x] 1.3 新增弯曲双向箭头图标资源，并确保图标按钮可点击区域不小于 48dp

## 2. 主页面结果卡片

- [x] 2.1 改造 `activity_main.xml`，移除结果表格表头依赖，新增结果区排序控件、状态区域和卡片列表容器
- [x] 2.2 改造 `item_bus_route.xml`，将五列行布局替换为路线结果卡片布局
- [x] 2.3 在结果卡片中突出路线和候车时间，并展示价格、总耗时和步行距离的用户可读单位
- [x] 2.4 调整结果卡片字号、行高、padding 和间距，使常见竖屏首屏大约显示 5 到 6 条完整结果且文字不重叠
- [x] 2.5 更新 `BusRouteAdapter` 绑定逻辑，支持卡片字段文案、候车时间查询中/不可用状态和辅助信息展示

## 3. 主页面排序和状态交互

- [x] 3.1 更新 `MainActivity` 的 view binding，移除表头 TextView 排序绑定，改为绑定显式排序控件
- [x] 3.2 保留并迁移按路线、价格、总耗时、候车时间和步行距离排序的行为及方向展示
- [x] 3.3 将查询中、无结果和查询失败状态迁移为统一状态卡或状态区域
- [x] 3.4 为查询结果出现、排序状态切换或状态变化增加 150ms 到 300ms 的轻量非阻塞反馈
- [x] 3.5 确认 ETA 渐进补全期间卡片可滚动、可排序，旧查询更新仍会被忽略

## 4. 路线管理页改造

- [x] 4.1 改造 `activity_route_manage.xml`，统一页面标题、新增路线入口、空状态和页面间距
- [x] 4.2 改造 `item_route_config.xml`，让路线配置卡片与主页面卡片风格保持一致
- [x] 4.3 调整编辑、克隆、删除按钮的视觉层级，删除操作保留危险色区分
- [x] 4.4 确认新增、编辑、克隆、删除和删除二次确认行为仍按既有逻辑工作

## 5. 路线编辑页交换控件

- [x] 5.1 改造 `activity_route_edit.xml`，用起终点输入框右侧中线附近的弯曲双向箭头图标按钮替代整行文字交换按钮
- [x] 5.2 为交换图标按钮设置“交换起点和终点”的无障碍说明，并避免遮挡输入框文本和下拉箭头
- [x] 5.3 更新 `RouteEditActivity` 绑定逻辑，复用现有 `swapPlaces()` 并增加 150ms 到 250ms 的轻量点击反馈
- [x] 5.4 确认交换已选择地点、交换未确认文本、保存校验、重复路线校验和地点搜索加载反馈仍按既有要求工作

## 6. 文档和项目约定

- [x] 6.1 确认 `docs/ui-style-guide.md` 已记录本次采用的主风格、结果卡片、状态卡、管理页和编辑页交换控件约定
- [x] 6.2 确认 `AGENTS.md` 引用 `docs/ui-style-guide.md`，后续 UI OpenSpec 默认遵循该风格指南

## 7. 验证

- [x] 7.1 运行 `openspec validate modernize-route-ui-system --strict`
- [x] 7.2 运行 `./gradlew build`
- [x] 7.3 如有可用设备或模拟器，截图验证主页面未查询、查询结果、查询状态、路线管理页和路线编辑页没有文字重叠、布局跳动或不可辨识按钮
- [x] 7.4 如无可用设备或模拟器，记录未完成手动视觉验证的原因
