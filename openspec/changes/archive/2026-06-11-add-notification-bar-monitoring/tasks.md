## 1. 前置檢查與模型準備

- [x] 1.1 確認 `show-multiple-citybus-eta-arrivals` 的實作狀態；若尚未可用，明確本變更先使用第 1 班 ETA `+2 分鐘` fallback 停止，並保留第 2 班 ETA 接口銜接點。
- [x] 1.2 新增通知欄監控 session 資料模型，保存路線結果、常用路線名稱或查詢上下文、首程 ETA query、上車站、步行到站分鐘數、語音開關、當前狀態和最後更新時間。
- [x] 1.3 新增步行速度預設模型，包含 `慢行 3.2`、`帶小孩 3.5`、`一般 5.0`、`快走 6.0` 並保證互斥選擇。
- [x] 1.4 新增場景修正模型，包含 `雨天 ×80%`、`等電梯 +2`、`天橋/過馬路 +2` 並支持多選疊加。
- [x] 1.5 新增監控狀態模型，覆蓋 `準備出門`、`立即出門`、`快遲到了`、資料延遲或暫無 ETA 狀態。

## 2. 步行到站估算

- [x] 2.1 擴展 Citybus 路線詳情解析能力，從 `getp2pstopinroute.php` 響應中解析起點到首程上車站的 `步行距離(約) N米`。
- [x] 2.2 基於起點坐標與首程上車站坐標實作經緯度直線距離估算，不額外乘繞路係數。
- [x] 2.3 實作距離到分鐘的估算 helper，使用當前步行速度向上取整。
- [x] 2.4 實作 `雨天 ×80%` 速度修正，只調整速度，不調整距離。
- [x] 2.5 實作 `等電梯 +2`、`天橋/過馬路 +2` 固定分鐘修正。
- [x] 2.6 實作 `max(接口距離估算時間, 經緯度直線距離估算時間, 用戶調整時間)` 計算規則。
- [x] 2.7 為接口距離解析、直線距離估算、雨天速度修正、固定分鐘修正和 max 規則新增單元測試。

## 3. 結果卡片入口

- [x] 3.1 調整 `item_bus_route.xml`，在候車狀態附近加入低權重小鈴鐺圖示入口。
- [x] 3.2 保證小鈴鐺視覺尺寸約 20dp 到 22dp，外層觸控區不小於 48dp。
- [x] 3.3 更新 `BusRouteAdapter`，為小鈴鐺提供獨立點擊回調並避免觸發既有路線詳情點擊。
- [x] 3.4 為小鈴鐺補充無障礙描述，例如 `啟動通知欄監控`。
- [x] 3.5 在 ETA 查詢中或暫無車輛狀態下阻止啟動監控，並展示可理解提示。
- [x] 3.6 驗證窄屏、長路線名、長站點預覽和字體放大時小鈴鐺不與核心文字重疊。

## 4. 監控啟動底部面板

- [x] 4.1 新增 `BottomSheetDialog` 或等效元件，展示 `啟動通知欄監控`、路線摘要和 `步行到站` 設定。
- [x] 4.2 實作 `步行到站 [-] X [+] 分鐘` 控件，手動調整後重新應用 max 規則。
- [x] 4.3 實作互斥步行速度 chips，默認選中 `一般 5.0`。
- [x] 4.4 實作可多選場景修正 chips：`雨天 ×80%`、`等電梯 +2`、`天橋/過馬路 +2`。
- [x] 4.5 實作 `語音提醒` 開關，默認開啟，關閉後只影響本次監控 session。
- [x] 4.6 點擊 `開始監控` 前檢查通知權限；缺失時引導授權且不啟動無通知監控。
- [x] 4.7 確保底部面板使用現有安靜實用風格，主操作明確，設定項不擠壓或重疊。

## 5. 前台服務與通知

- [x] 5.1 在 manifest 中加入所需通知、前台服務和對應 Android 版本要求的聲明。
- [x] 5.2 新增通知渠道，區分常駐監控通知和狀態提醒所需的重要性。
- [x] 5.3 新增短時前台服務，啟動時立即顯示 BusIsComing 監控通知。
- [x] 5.4 服務每分鐘嘗試刷新首程 ETA，刷新時優先只請求 ETA，不重跑完整點到點路線搜尋。
- [x] 5.5 實作通知展示文案：監控狀態、路線號、候車剩餘、步行到站和最後更新時間。
- [x] 5.6 實作通知 action：`刷新` 立即刷新 ETA，`停止` 停止服務並移除通知，`打開 App` 回到主頁。
- [x] 5.7 實作資料延遲或刷新失敗時的通知降級展示，保留最後一次成功 ETA。
- [x] 5.8 實作前台服務資源釋放，確保停止時取消 tick、清理通知和釋放引用。

## 6. 狀態判斷、語音播報與停止規則

- [x] 6.1 實作剩餘可等待時間計算：`第 1 班 ETA 分鐘數 - 步行到站分鐘數`。
- [x] 6.2 實作狀態閾值：`> 2` 為 `準備出門`，`1..2` 為 `立即出門`，`<= 0` 為 `快遲到了`。
- [x] 6.3 實作 TextToSpeech 初始化、播報和釋放；TTS 失敗時不阻塞通知監控。
- [x] 6.4 實作三種狀態切換播報文案，包含當前汽車到站剩餘分鐘數。
- [x] 6.5 記錄上次播報狀態，確保同一狀態不重複播報。
- [x] 6.6 實作用戶手動停止規則。
- [x] 6.7 實作第 2 班 ETA 過站後自動停止；沒有第 2 班 ETA 時使用第 1 班過站後 `+2 分鐘` fallback 停止。
- [x] 6.8 實作連續刷新失敗保護，避免監控服務長時間無效常駐。

## 7. 測試與驗證

- [x] 7.1 補充監控狀態單元測試，覆蓋 `準備出門`、`立即出門`、`快遲到了` 邊界值。
- [x] 7.2 補充語音播報 helper 測試，覆蓋三種文案和同一狀態不重複播報。
- [x] 7.3 補充通知 session 和停止規則測試，覆蓋第 2 班 ETA 停止、第 1 班 `+2 分鐘` fallback 和手動停止。
- [x] 7.4 補充卡片入口 adapter 或 layout 測試，覆蓋小鈴鐺點擊不觸發路線詳情。
- [x] 7.5 補充底部面板可測試 helper 或 UI 驗證，覆蓋互斥步速、多選場景、語音開關和步行到站 max 規則。
- [x] 7.6 運行 `./gradlew testDebugUnitTest`。
- [x] 7.7 運行 `./gradlew build`。
- [x] 7.8 使用模擬器或真機驗證：小鈴鐺入口、底部面板、通知權限、常駐通知、刷新/停止/打開 App、狀態切換和語音播報。
- [x] 7.9 驗證息屏或鎖屏場景下通知仍可查看，並記錄「每分鐘嘗試更新」是否受系統限制影響。
- [x] 7.10 運行 `openspec validate add-notification-bar-monitoring --strict`。
