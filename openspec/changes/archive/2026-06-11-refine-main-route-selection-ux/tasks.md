## 1. 常用路線快捷卡展示

- [x] 1.1 將主頁快捷卡可見列表計算改為基於原始排序 Top 3 與當前選中路線的展示規則。
- [x] 1.2 當選中路線不在原始 Top 3 中時，將該路線臨時展示為第一張快捷卡，並用原始 Top 3 補齊剩餘位置。
- [x] 1.3 確保重新選中另一條非原始 Top 3 路線時，上一條臨時提升路線不再保留在首頁快捷卡中。
- [x] 1.4 調整快捷卡選中態為明顯邊框與淺色背景，不展示勾選圖示或 `目前` 標籤。
- [x] 1.5 確保臨時提升只影響首頁展示，不改變 `RouteConfigRepository` 讀取排序、使用次數或最近使用時間。

## 2. 完整常用路線入口

- [x] 2.1 將 `常用路線` 標題改為左標題、右側 `全部` 文字入口的水平佈局。
- [x] 2.2 移除或隱藏常用路線卡片右側獨立 `>` 展開箭頭按鈕。
- [x] 2.3 將 `全部` 入口綁定到現有完整常用路線列表底部彈層。
- [x] 2.4 確保完整常用路線列表按真實使用排序展示所有路線，不受首頁臨時提升展示影響。
- [x] 2.5 確保完整列表最後仍展示次要樣式的 `查詢臨時起點和終點` 入口。

## 3. 臨時查詢結果上下文

- [x] 3.1 在主頁結果區新增或復用一個單行臨時查詢上下文條，默認隱藏。
- [x] 3.2 在臨時查詢發起時保存起點和終點快照，並於 loading、成功、失敗和無結果狀態中顯示上下文條。
- [x] 3.3 將上下文條內容限制為 `臨時` 標識、單行省略的 `起點 -> 終點` 和 `保存` 操作。
- [x] 3.4 在常用路線查詢、切換常用路線或清空結果時隱藏臨時查詢上下文條。
- [x] 3.5 確保常用路線查詢結果上方不新增 `目前查詢` 摘要。

## 4. 結果區排序控件

- [x] 4.1 移除或隱藏排序 chips 上方獨立的 `排序` 標題文字。
- [x] 4.2 保持排序 chips 的字段文案、選中態、方向標識和點擊排序行為不變。
- [x] 4.3 檢查臨時查詢上下文條、排序 chips、結果摘要和結果列表之間的垂直間距，確保結果區保持緊湊。

## 5. 臨時查詢保存流程

- [x] 5.1 讓臨時上下文條的 `保存` 操作復用臨時查詢保存為常用的命名、校驗、重複檢查和 repository 寫入流程。
- [x] 5.2 保存成功後刷新常用路線列表，選中新保存路線，並按常用路線選中態更新主頁。
- [x] 5.3 確保保存操作使用臨時查詢發起時的起終點快照，而不是後續輸入框或路線選擇狀態。

## 6. 測試與驗證

- [x] 6.1 添加或更新單元測試覆蓋快捷卡可見列表計算：原始 Top 3、選中 Top 3、選中非 Top 3、重新選中另一條非 Top 3。
- [x] 6.2 添加或更新 UI/集成測試覆蓋 `全部` 入口打開完整列表和臨時查詢入口位於列表最後。
- [x] 6.3 添加或更新測試覆蓋臨時查詢上下文條顯示、隱藏、單行內容和保存入口行為。
- [x] 6.4 添加或更新 UI/集成測試覆蓋排序 chips 上方不展示獨立 `排序` 標題。
- [x] 6.5 執行 `openspec validate refine-main-route-selection-ux --strict`。
- [x] 6.6 執行 `./gradlew build`。
- [x] 6.7 如有可用設備或模擬器，手動驗證常用路線選中態、非 Top 3 臨時提升、`全部` 入口、臨時查詢上下文、排序控件密度和保存流程。
