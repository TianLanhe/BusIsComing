## 1. 共用狀態與量度策略

- [x] 1.1 新增可單測的站名自然寬度／32–68% 分配策略，覆蓋兩端短、一短一長、兩端長與極端文字。
- [x] 1.2 新增搜尋結果保存資格與目前位置 generation 狀態策略，覆蓋輸入、交換、過期回調、空結果及失敗。
- [x] 1.3 建立單一正式乘車碼 explicit intent action 與 shortcut intent 建構邊界，復用既有 provider 回退鏈。

## 2. 路線卡片與底部導航

- [x] 2.1 把路線卡片站名改為既有左側文字列內的起點、固定箭頭及終點自適應單行容器，保留完整無障礙描述。
- [x] 2.2 更新卡片 binder 與漸進補齊行為，確保三語站名、不可用預覽及非同步更新不改變站名行數。
- [x] 2.3 把底部導航改為固定 24dp 圖示、64×32dp 選中膠囊、12sp Regular／13sp Bold 標籤與約 150ms 持續選中態。
- [x] 2.4 更新路線卡片與底部導航的 JVM／XML contract 測試，驗證量度、單行、省略、無障礙和不跳動契約。

## 3. 常用頁結構

- [x] 3.1 恢復「常用行程／全部／管理」同一水平列，移除常用頁乘車碼與首次空狀態的搜尋次入口。
- [x] 3.2 收斂常用頁至 16dp 水平邊距、8–12dp 區段節奏，同時保留至少 48dp 觸控目標。
- [x] 3.3 以單一巢狀捲動體系讓常用行程和查詢控制捲走，只讓排序及結果摘要吸頂，並維持下拉刷新。
- [x] 3.4 更新常用頁狀態與佈局測試，覆蓋空狀態、一般結果、長列表捲動及隱藏控制不保留空白。

## 4. 搜尋頁輸入與結果

- [x] 4.1 移除搜尋大標題與重複結果上下文／編輯入口，修正輸入起始內距、末端工具空間、光標、焦點及欄位級輔助文案。
- [x] 4.2 把「存為常用行程」移至左側輸入欄，僅在目前起終點對應的非空成功結果時顯示並沿用既有保存對話框。
- [x] 4.3 讓搜尋結果依序使用共用排序、同列路線數量／更新時間與結果卡片，並與常用頁視覺契約一致。
- [x] 4.4 實作每個主畫面實例首次、非阻塞的定位＋Reverse Geocoding；起點互動／交換／離頁／語言變更使舊回調失效，終點互動不取消。
- [x] 4.5 更新搜尋頁 JVM、XML 與狀態測試，覆蓋保存資格、輸入可用性、自動／手動定位、失敗輔助文案及回調競爭。

## 5. 乘車碼系統與情境入口

- [x] 5.1 在 Manifest 與 shortcut XML 發佈三語靜態「乘車碼」App Shortcut，並由主畫面消費共用 intent action。
- [x] 5.2 在設定頁新增「乘車碼快捷方式／從桌面一按開啟」列，透過相容 API 請求 pinned shortcut 並處理不支援狀態。
- [x] 5.3 在候車前台通知加入第三個乘車碼 action，確認點擊不刷新、不停止且失敗沿用既有提示。
- [x] 5.4 新增 shortcut metadata、設定入口、通知 action、共用 provider 回退及查詢／監控狀態隔離測試。

## 6. 多語言、主題與無障礙

- [x] 6.1 為所有新增或修改的 UI、Toast、shortcut、通知與 contentDescription 提供繁體、簡體及英文資源。
- [x] 6.2 完成淺色／深色語義色與焦點光標、導航膠囊、排序、保存及定位錯誤狀態的資源適配。
- [x] 6.3 在可用裝置或模擬器驗證三語×深淺色、360dp、font scale 1.0／1.3／2.0、常用頁吸頂與三個乘車碼入口；記錄無設備限制。

## 7. 最終驗證與交付

- [x] 7.1 執行相關 JVM／instrumentation contract 測試並修復所有回歸。
- [x] 7.2 執行 `openspec validate refine-route-ui-interactions` 並確認 change 有效。
- [x] 7.3 執行 `./gradlew build`，確保編譯、單元測試、lint 及 debug／release assemble 全部通過。
- [x] 7.4 檢查全部 task、`git status --short` 與提交範圍，依專案規則提交本次 apply 改動。
