## 1. 数据模型与本地存储

- [x] 1.1 新增 `Place` 值对象，包含地点名称、纬度和经度。
- [x] 1.2 将 `RouteConfig` 的起点和终点从纯文本调整为 `Place`。
- [x] 1.3 将路线配置数据库表升级为保存起点/终点名称、纬度和经度的字段结构。
- [x] 1.4 将数据库版本升级到新版本，并在升级时删除旧 `route_configs` 表后重建新表。
- [x] 1.5 更新 `RouteConfigRepository` 的新增、修改、删除、查询映射逻辑，确保读写完整地点信息。
- [x] 1.6 增加或调整本地存储测试，覆盖保存和读取路线地点名称、纬度、经度。
- [x] 1.7 增加或调整数据库升级测试，覆盖旧纯文本路线表被删除并重建为新地点表。

## 2. Citybus 地点搜索能力

- [x] 2.1 新增 `PlaceSearchRepository` 接口，定义按关键词搜索地点候选的能力。
- [x] 2.2 新增 `CitybusPlaceSearchRepository`，使用 `HttpURLConnection` 调用 Citybus 地点搜索接口。
- [x] 2.3 在地点搜索请求中携带 `l=0`、`q`、`limit=100`、`timestamp` 参数，并对关键词做 URL 编码。
- [x] 2.4 在地点搜索请求中设置用户提供 cURL 中的 headers，包括 Cookie、Referer、User-Agent 和 `X-Requested-With`。
- [x] 2.5 新增 `CitybusPlaceParser`，将 Citybus 纯文本响应解析为 `Place` 列表。
- [x] 2.6 为 parser 增加正常响应测试，验证第 2 列名称、第 3 列纬度、第 4 列经度被正确解析。
- [x] 2.7 为 parser 增加 `No Result` 响应测试，验证返回空候选列表。
- [x] 2.8 为 parser 增加空响应、错误格式和部分无效行测试，验证错误与跳过规则符合规格。
- [x] 2.9 为地点搜索仓库增加请求构造测试或可替代验证，确认 URL 参数和关键 headers 符合规格。

## 3. 路线新增和编辑交互

- [x] 3.1 将路线新增/编辑页的起点和终点输入控件改为可输入下拉选择控件。
- [x] 3.2 为起点和终点输入实现 1 个字符起搜和 300ms debounce。
- [x] 3.3 搜索成功时在下拉列表展示接口返回的候选地点，最多展示 100 条。
- [x] 3.4 用户选择候选地点后，记录完整 `Place` 并在输入框中展示地点名称。
- [x] 3.5 用户手动修改已选择地点文本后，清空对应已选择 `Place`，要求重新从候选中选择。
- [x] 3.6 搜索无候选时展示“没有匹配地点”状态。
- [x] 3.7 网络失败或解析失败时展示“地点搜索失败，请稍后重试”状态。
- [x] 3.8 保存前校验路线名称、已选择起点地点、已选择终点地点均有效。
- [x] 3.9 保存前校验起点和终点不能完全相同，完全相同定义为名称、纬度、经度都一致。
- [x] 3.10 编辑已有路线时预填路线名称、起点地点名称和终点地点名称，并保留已保存的 `Place` 作为当前选择。
- [x] 3.11 增加或调整路线编辑测试，覆盖未选择候选、修改文本后清空选择、起终点相同和有效保存。

## 4. 主界面、管理页与查询集成

- [x] 4.1 更新路线管理页列表展示，只显示路线名称和 `起点地点名称 -> 终点地点名称`。
- [x] 4.2 更新主界面路线下拉展示，只显示 `路线名称：起点地点名称 -> 终点地点名称`。
- [x] 4.3 调整 `BusRouteRepository` 查询接口，使查询流程接收起点和终点地点信息或经纬度。
- [x] 4.4 更新主界面查询逻辑，点击查询时向巴士查询仓库传递已选路线的起点和终点经纬度。
- [x] 4.5 更新 `MockBusRouteRepository`，忽略任意起点和终点并固定返回测试巴士路线数据。
- [x] 4.6 增加或调整查询集成测试，覆盖主界面选择路线后使用地点信息查询，以及 Mock 固定返回。

## 5. 验证与回归

- [x] 5.1 执行 `./gradlew build`，确认编译、单元测试、lint 和 assemble 通过。
- [x] 5.2 手动验证新增路线：输入 1 个字后触发搜索，候选下拉可滚动展示，选择后可保存。
- [x] 5.3 手动验证编辑路线：已有地点名称正确预填，修改文本后必须重新选择候选。
- [x] 5.4 手动验证保存校验：未选择起点、未选择终点、起终点完全相同时均不能保存。
- [x] 5.5 手动验证异常状态：无候选显示“没有匹配地点”，网络或解析失败显示“地点搜索失败，请稍后重试”。
- [x] 5.6 手动验证主界面和路线管理页只展示地点名称，不展示经纬度。
- [x] 5.7 手动验证选择任意已保存路线后点击查询，表格展示固定 Mock 巴士路线数据并保留排序功能。
- [x] 5.8 如当前没有可用模拟器或真机，记录未完成的设备端视觉验证范围。

设备端验证记录：已在 `Pixel_8(AVD)` 上完成回归验证。通过粘贴中文关键词验证新增路线输入后触发 Citybus 地点搜索、候选下拉可滚动展示、选择候选后下拉关闭且不重复弹出，并成功保存路线 `丰兴楼：興華邨2豐興樓 -> 豐興樓`。编辑页验证已有地点名称正确预填；修改已选起点文本为不可匹配关键词后，保存提示“请选择起点地点”，确认必须重新选择候选。保存校验已覆盖空表单“请选择起点地点/请选择终点地点”和起终点完全相同时的“起点和终点不能相同”。异常状态已覆盖无候选“没有匹配地点”，以及临时关闭模拟器网络后的“地点搜索失败，请稍后重试”。主界面和路线管理页均只展示地点名称，未展示经纬度。选择已保存路线查询后固定 Mock 结果展示 `82/HK$6.0/约 4 分钟`、`8X/HK$7.2/约 9 分钟`、`780/HK$10.5/约 13 分钟`，价格和预计等候时间表头均验证可切换升序/降序排序。`connectedDebugAndroidTest` 通过。
