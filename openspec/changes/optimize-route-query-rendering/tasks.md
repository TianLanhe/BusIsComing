## 1. Repository 渐进查询流程

- [x] 1.1 调整 `CitybusBusRouteRepository.searchRoutesProgressively`，在 `searchRoutes` 完成聚合排序后立即调用 `onInitialRoutes`，不再等待首屏 ETA。
- [x] 1.2 保持初始路线候车状态：可解析首程 ETA 的路线为 `WaitTimeState.Loading`，不可解析首程 ETA 的路线为 `WaitTimeState.Unavailable`。
- [x] 1.3 将 ETA 补全从当前路线查询同步流程中拆出，改为独立、有并发上限的后台任务继续执行。
- [x] 1.4 调整 ETA 补全回调顺序，确保单个 ETA 请求完成后尽快触发对应路线更新，不被提交顺序中较慢的 ETA 请求阻塞。

## 2. 旧 ETA 任务取消和新查询隔离

- [x] 2.1 为后台 ETA 补全任务建立可取消句柄或等价的查询作用域，支持在新查询、切换路线、清空结果和页面销毁时取消旧任务。
- [x] 2.2 确保新一轮路线查询可以立即进入 `queryExecutor` 执行，不等待上一轮剩余 ETA 补全完成。
- [x] 2.3 保留并强化 `querySequence` 或等价 token 校验，确保旧 ETA 任务即使完成也不会更新当前 UI。
- [x] 2.4 确认 Activity 销毁时所有后台查询和 ETA 任务都会停止调度或被安全忽略，避免泄漏回调。

## 3. 主界面状态和排序

- [x] 3.1 确认主界面在收到初始路线列表后立即结束“查询中”状态并展示结果卡片。
- [x] 3.2 确认默认排序仍为总耗时升序，并且后台 ETA 更新不会改变默认总耗时排序。
- [x] 3.3 确认当前排序字段为候车时间时，后台 ETA 更新后仍按候车时间规则重新排序。
- [x] 3.4 确认无结果和路线查询失败状态不受 ETA 后台任务改造影响。

## 4. 单元测试

- [x] 4.1 更新原有“前 5 条 ETA 完成后展示”相关测试，改为验证初始路线列表不等待 ETA 即可发出。
- [x] 4.2 增加测试：ETA resolver 被阻塞或延迟时，`onInitialRoutes` 仍先于 ETA 更新触发，初始候车状态为查询中。
- [x] 4.3 增加测试：多条 ETA 并发补全时，后提交但先完成的 ETA 可以先回调更新。
- [x] 4.4 增加测试：连续发起两次查询时，第二次查询不会等待第一次剩余 ETA 补全完成。
- [x] 4.5 增加测试：旧查询 ETA 完成后不会更新新查询结果。
- [x] 4.6 保留 ETA 失败隔离、相同首程 ETA 去重、不可解析首程 ETA 标记不可用等既有行为测试。

## 5. 端到端验证

- [x] 5.1 运行 `./gradlew build`，确认编译、单元测试、lint 和 assemble 通过。
- [x] 5.2 使用模拟器或真机查询一条已有路线，确认路线结果在 ETA 全部完成前已经展示，候车时间随后逐条补全。
- [x] 5.3 在上一轮候车时间仍显示查询中时再次点击查询，确认新查询立即进入查询中状态并最终展示新结果。
- [x] 5.4 使用 logcat 或临时诊断日志确认新查询发起时间不依赖旧 ETA 补全结束时间。
